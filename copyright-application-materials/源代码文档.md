# 心灵絮语软件 V2.0 源代码文档

**著作权人:** [著作权人姓名]

本源代码文档包含前30页和后30页源代码，每页50行，共3000行。

---

## 源代码文件列表

- **app.py**: 主应用，用户、世界、文件上传、聊天等功能
- **llm_manager.py**: LLM管理器，支持多AI模型
- **models.py**: 数据库模型定义
- **config.py**: 配置模块
- **world-config.js**: 世界配置模块，文件上传和分享功能
- **chat.js**: 聊天模块，WebSocket通信
- **component.js**: UI组件和交互控制
- **index.html**: HTML主页面
- **init.js**: 初始化脚本

---

## 前30页源代码 (第1-1500行)

### 文件: app.py

**路径:** ../backend/app.py  
**说明:** 主应用，用户、世界、文件上传、聊天等功能  
**行数:** 808行

```
     1  """
     2  心灵絮语后端主应用
     3  """
     4  from fastapi import FastAPI, WebSocket, WebSocketDisconnect, HTTPException, UploadFile, File, Form
     5  from fastapi.middleware.cors import CORSMiddleware
     6  from fastapi.staticfiles import StaticFiles
     7  from sqlmodel import Session, select
     8  from pydantic import BaseModel
     9  from typing import List, Dict, Optional
    10  from datetime import datetime
    11  import os
    12  import uuid
    13  import shutil
    14  import keyring
    15  from anthropic import Anthropic, DefaultHttpxClient
    16  import httpx
    17  import jieba
    18  import asyncio
    19  
    20  from config import engine, settings, init_db, reset_db, get_db_info
    21  from models import User, World, Chat, UploadRecord, Feedback, WorldShare, FeedbackType
    22  from llm_manager import llm_manager
    23  
    24  # 设置工作目录
    25  os.chdir(os.path.dirname(__file__))
    26  
    27  # 初始化数据库
    28  init_db()
    29  
    30  # 确保上传目录存在
    31  os.makedirs(settings.UPLOAD_DIR, exist_ok=True)
    32  
    33  # ==================== LLM管理器已在llm_manager.py中初始化 ====================
    34  # 检查LLM是否可用
    35  if not llm_manager.get_available_llms():
    36      print("警告：没有可用的LLM配置，聊天功能将不可用")
    37  
    38  # ==================== FastAPI应用 ====================
    39  
    40  app = FastAPI(
    41      title="心灵絮语API",
    42      description="心灵絮语后端服务",
    43      version="2.0.0"
    44  )
    45  
    46  app.add_middleware(
    47      CORSMiddleware,
    48      allow_origins=["*"],
    49      allow_credentials=True,
    50      allow_methods=["*"],
```

===== 第2页 =====

```
    51      allow_headers=["*"],
    52  )
    53  
    54  # 静态文件服务（上传文件）
    55  app.mount("/uploads", StaticFiles(directory=settings.UPLOAD_DIR), name="uploads")
    56  
    57  # ==================== 请求模型 ====================
    58  
    59  class UsernameRequest(BaseModel):
    60      username: str
    61  
    62  class WorldCreateRequest(BaseModel):
    63      name: str = "我的世界"
    64      system_prompt: Optional[str] = None
    65      greeting_message: Optional[str] = None
    66  
    67  class WorldUpdateRequest(BaseModel):
    68      name: Optional[str] = None
    69      system_prompt: Optional[str] = None
    70      greeting_message: Optional[str] = None
    71      is_public: Optional[bool] = None
    72  
    73  class FeedbackRequest(BaseModel):
    74      feedback_type: str  # "feature_request" 或 "bug_report"
    75      title: str
    76      content: str
    77  
    78  # ==================== 全局状态 ====================
    79  
    80  active_conversations: Dict[str, List[Dict]] = {}
    81  usernames: Dict[str, str] = {}
    82  current_username: str = None
    83  
    84  # 默认系统提示词
    85  with open("system_prompt_en.md", "r", encoding="utf-8") as file:
    86      default_system_prompt = file.read().strip()
    87  
    88  # ==================== 用户相关路由 ====================
    89  
    90  @app.post("/api/users/register")
    91  async def register_user(request: UsernameRequest):
    92      """用户注册/登录（无密码系统）"""
    93      global current_username
    94      current_username = request.username
    95  
    96      with Session(engine) as session:
    97          existing_user = session.exec(
    98              select(User).where(User.username == request.username)
    99          ).first()
   100  
```

===== 第3页 =====

```
   101          if existing_user:
   102              return {
   103                  "message": f"欢迎回来，{request.username}！",
   104                  "user_id": existing_user.id,
   105                  "is_new": False
   106              }
   107          else:
   108              new_user = User(username=request.username)
   109              session.add(new_user)
   110              session.commit()
   111              session.refresh(new_user)
   112  
   113              # 为新用户创建默认世界
   114              default_world = World(
   115                  user_id=new_user.id,
   116                  name="默认世界",
   117                  greeting_message="欢迎来到这片充满魔法的森林！(^_^) 我是森林精灵絮语，很高兴遇见你~"
   118              )
   119              session.add(default_world)
   120              session.commit()
   121  
   122              return {
   123                  "message": f"欢迎加入，{request.username}！",
   124                  "user_id": new_user.id,
   125                  "is_new": True
   126              }
   127  
   128  # 兼容旧接口
   129  @app.post("/set_username/")
   130  async def set_username(request: UsernameRequest):
   131      return await register_user(request)
   132  
   133  @app.get("/api/users/{username}")
   134  async def get_user(username: str):
   135      """获取用户信息"""
   136      with Session(engine) as session:
   137          user = session.exec(select(User).where(User.username == username)).first()
   138          if not user:
   139              raise HTTPException(status_code=404, detail="用户未找到")
   140          return {
   141              "id": user.id,
   142              "username": user.username,
   143              "created_at": user.created_at
   144          }
   145  
   146  # ==================== 世界配置路由 ====================
   147  
   148  @app.get("/api/worlds/{user_id}")
   149  async def get_user_worlds(user_id: int):
   150      """获取用户的所有世界"""
```

===== 第4页 =====

```
   151      with Session(engine) as session:
   152          worlds = session.exec(select(World).where(World.user_id == user_id)).all()
   153          return [
   154              {
   155                  "id": w.id,
   156                  "name": w.name,
   157                  "sky_texture": w.sky_texture,
   158                  "fairy_model": w.fairy_model,
   159                  "background_music": w.background_music,
   160                  "system_prompt": w.system_prompt,
   161                  "greeting_message": w.greeting_message,
   162                  "is_public": w.is_public,
   163                  "created_at": w.created_at
   164              }
   165              for w in worlds
   166          ]
   167  
   168  @app.post("/api/worlds/{user_id}")
   169  async def create_world(user_id: int, request: WorldCreateRequest):
   170      """创建新世界"""
   171      with Session(engine) as session:
   172          user = session.get(User, user_id)
   173          if not user:
   174              raise HTTPException(status_code=404, detail="用户未找到")
   175  
   176          world = World(
   177              user_id=user_id,
   178              name=request.name,
   179              system_prompt=request.system_prompt,
   180              greeting_message=request.greeting_message
   181          )
   182          session.add(world)
   183          session.commit()
   184          session.refresh(world)
   185  
   186          return {"message": "世界创建成功", "world_id": world.id}
   187  
   188  @app.put("/api/worlds/{world_id}")
   189  async def update_world(world_id: int, request: WorldUpdateRequest):
   190      """更新世界配置"""
   191      with Session(engine) as session:
   192          world = session.get(World, world_id)
   193          if not world:
   194              raise HTTPException(status_code=404, detail="世界未找到")
   195  
   196          if request.name is not None:
   197              world.name = request.name
   198          if request.system_prompt is not None:
   199              world.system_prompt = request.system_prompt
   200          if request.greeting_message is not None:
```

===== 第5页 =====

```
   201              world.greeting_message = request.greeting_message
   202          if request.is_public is not None:
   203              world.is_public = request.is_public
   204  
   205          world.updated_at = datetime.utcnow()
   206          session.add(world)
   207          session.commit()
   208  
   209          return {"message": "世界更新成功"}
   210  
   211  @app.get("/api/worlds/detail/{world_id}")
   212  async def get_world_detail(world_id: int):
   213      """获取世界详情"""
   214      with Session(engine) as session:
   215          world = session.get(World, world_id)
   216          if not world:
   217              raise HTTPException(status_code=404, detail="世界未找到")
   218  
   219          return {
   220              "id": world.id,
   221              "name": world.name,
   222              "sky_texture": world.sky_texture,
   223              "fairy_model": world.fairy_model,
   224              "background_music": world.background_music,
   225              "system_prompt": world.system_prompt,
   226              "greeting_message": world.greeting_message,
   227              "is_public": world.is_public,
   228              "user_id": world.user_id
   229          }
   230  
   231  # ==================== 文件上传路由 ====================
   232  
   233  def validate_file_type(filename: str, allowed_types: set) -> bool:
   234      """验证文件类型"""
   235      ext = os.path.splitext(filename)[1].lower()
   236      return ext in allowed_types
   237  
   238  def save_upload_file(file: UploadFile, user_id: int, file_type: str) -> str:
   239      """保存上传文件并返回相对路径"""
   240      ext = os.path.splitext(file.filename)[1].lower()
   241      unique_filename = f"{user_id}_{file_type}_{uuid.uuid4().hex}{ext}"
   242      user_dir = os.path.join(settings.UPLOAD_DIR, str(user_id))
   243      os.makedirs(user_dir, exist_ok=True)
   244  
   245      file_path = os.path.join(user_dir, unique_filename)
   246      with open(file_path, "wb") as buffer:
   247          shutil.copyfileobj(file.file, buffer)
   248  
   249      return f"{user_id}/{unique_filename}"
   250  
```

===== 第6页 =====

```
   251  @app.post("/api/upload/sky-texture/{world_id}")
   252  async def upload_sky_texture(world_id: int, file: UploadFile = File(...)):
   253      """上传天空贴图"""
   254      if not validate_file_type(file.filename, settings.ALLOWED_IMAGE_TYPES):
   255          raise HTTPException(status_code=400, detail="不支持的图片格式")
   256  
   257      with Session(engine) as session:
   258          world = session.get(World, world_id)
   259          if not world:
   260              raise HTTPException(status_code=404, detail="世界未找到")
   261  
   262          relative_path = save_upload_file(file, world.user_id, "sky")
   263          world.sky_texture = relative_path
   264          world.updated_at = datetime.utcnow()
   265  
   266          # 记录上传
   267          record = UploadRecord(
   268              user_id=world.user_id,
   269              world_id=world_id,
   270              file_type="sky_texture",
   271              original_filename=file.filename,
   272              stored_filename=relative_path.split("/")[-1],
   273              file_path=relative_path,
   274              file_size=file.size or 0
   275          )
   276          session.add(record)
   277          session.add(world)
   278          session.commit()
   279  
   280          return {"message": "天空贴图上传成功", "path": f"/uploads/{relative_path}"}
   281  
   282  @app.post("/api/upload/fairy-model/{world_id}")
   283  async def upload_fairy_model(world_id: int, file: UploadFile = File(...)):
   284      """上传精灵模型"""
   285      if not validate_file_type(file.filename, settings.ALLOWED_MODEL_TYPES):
   286          raise HTTPException(status_code=400, detail="不支持的模型格式")
   287  
   288      with Session(engine) as session:
   289          world = session.get(World, world_id)
   290          if not world:
   291              raise HTTPException(status_code=404, detail="世界未找到")
   292  
   293          relative_path = save_upload_file(file, world.user_id, "fairy")
   294          world.fairy_model = relative_path
   295          world.updated_at = datetime.utcnow()
   296  
   297          record = UploadRecord(
   298              user_id=world.user_id,
   299              world_id=world_id,
   300              file_type="fairy_model",
```

===== 第7页 =====

```
   301              original_filename=file.filename,
   302              stored_filename=relative_path.split("/")[-1],
   303              file_path=relative_path,
   304              file_size=file.size or 0
   305          )
   306          session.add(record)
   307          session.add(world)
   308          session.commit()
   309  
   310          return {"message": "精灵模型上传成功", "path": f"/uploads/{relative_path}"}
   311  
   312  @app.post("/api/upload/background-music/{world_id}")
   313  async def upload_background_music(world_id: int, file: UploadFile = File(...)):
   314      """上传背景音乐"""
   315      if not validate_file_type(file.filename, settings.ALLOWED_AUDIO_TYPES):
   316          raise HTTPException(status_code=400, detail="不支持的音频格式")
   317  
   318      with Session(engine) as session:
   319          world = session.get(World, world_id)
   320          if not world:
   321              raise HTTPException(status_code=404, detail="世界未找到")
   322  
   323          relative_path = save_upload_file(file, world.user_id, "music")
   324          world.background_music = relative_path
   325          world.updated_at = datetime.utcnow()
   326  
   327          record = UploadRecord(
   328              user_id=world.user_id,
   329              world_id=world_id,
   330              file_type="background_music",
   331              original_filename=file.filename,
   332              stored_filename=relative_path.split("/")[-1],
   333              file_path=relative_path,
   334              file_size=file.size or 0
   335          )
   336          session.add(record)
   337          session.add(world)
   338          session.commit()
   339  
   340          return {"message": "背景音乐上传成功", "path": f"/uploads/{relative_path}"}
   341  
   342  @app.post("/api/upload/system-prompt/{world_id}")
   343  async def upload_system_prompt(world_id: int, file: UploadFile = File(...)):
   344      """上传系统提示词文件"""
   345      if not validate_file_type(file.filename, settings.ALLOWED_TEXT_TYPES):
   346          raise HTTPException(status_code=400, detail="不支持的文本格式")
   347  
   348      with Session(engine) as session:
   349          world = session.get(World, world_id)
   350          if not world:
```

===== 第8页 =====

```
   351              raise HTTPException(status_code=404, detail="世界未找到")
   352  
   353          # 读取文件内容
   354          content = await file.read()
   355          world.system_prompt = content.decode("utf-8")
   356          world.updated_at = datetime.utcnow()
   357  
   358          record = UploadRecord(
   359              user_id=world.user_id,
   360              world_id=world_id,
   361              file_type="system_prompt",
   362              original_filename=file.filename,
   363              stored_filename=file.filename,
   364              file_path="",
   365              file_size=len(content)
   366          )
   367          session.add(record)
   368          session.add(world)
   369          session.commit()
   370  
   371          return {"message": "系统提示词上传成功"}
   372  
   373  # ==================== 聊天路由 ====================
   374  
   375  @app.websocket("/ws/chat_with_claude_via_stream/")
   376  async def chat_with_claude_websocket_via_stream(websocket: WebSocket, world_id: Optional[int] = None):
   377      """WebSocket流式聊天（支持多LLM）"""
   378      await websocket.accept()
   379      client_id = f"{websocket.client.host}:{websocket.client.port}"
   380  
   381      # 获取世界配置
   382      system_prompt = default_system_prompt
   383      greeting = "欢迎来到这片充满魔法的森林！(^_^) 我是森林精灵絮语，很高兴遇见你~"
   384  
   385      if world_id:
   386          with Session(engine) as session:
   387              world = session.get(World, world_id)
   388              if world:
   389                  if world.system_prompt:
   390                      system_prompt = world.system_prompt
   391                  if world.greeting_message:
   392                      greeting = world.greeting_message
   393  
   394      active_conversations[client_id] = [
   395          {"role": "assistant", "content": greeting}
   396      ]
   397      usernames[client_id] = current_username or "Unknown"
   398  
   399      with Session(engine) as session:
   400          user = session.exec(
```

===== 第9页 =====

```
   401              select(User).where(User.username == usernames[client_id])
   402          ).first()
   403          if not user:
   404              user = User(username="Unknown")
   405              session.add(user)
   406              session.commit()
   407              session.refresh(user)
   408  
   409      try:
   410          while True:
   411              data = await websocket.receive_json()
   412              user_message = data.get("content", "")
   413              llm_id = data.get("llm_id")  # 可选的LLM ID
   414              model = data.get("model")  # 可选的模型名称
   415              max_tokens = data.get("max_tokens", 2048)
   416  
   417              with Session(engine) as session:
   418                  chat = Chat(user_id=user.id, world_id=world_id, role="user", content=user_message)
   419                  session.add(chat)
   420                  session.commit()
   421  
   422              active_conversations[client_id].append(
   423                  {"role": "user", "content": user_message}
   424              )
   425  
   426              if not llm_manager.get_available_llms():
   427                  await websocket.send_text("抱歉，聊天服务暂时不可用")
   428                  await websocket.send_text("[END_OF_RESPONSE]")
   429                  continue
   430  
   431              # 使用LLM管理器的流式聊天
   432              partial_response = ""
   433              for text in llm_manager.chat_stream(
   434                  messages=active_conversations[client_id],
   435                  system_prompt=system_prompt,
   436                  llm_id=llm_id,
   437                  model=model,
   438                  max_tokens=max_tokens
   439              ):
   440                  partial_response += text
   441                  await websocket.send_text(text)
   442  
   443              active_conversations[client_id].append(
   444                  {"role": "assistant", "content": partial_response}
   445              )
   446              with Session(engine) as session:
   447                  chat = Chat(
   448                      user_id=user.id, world_id=world_id, role="assistant", content=partial_response
   449                  )
   450                  session.add(chat)
```

===== 第10页 =====

```
   451                  session.commit()
   452  
   453              await websocket.send_text("[END_OF_RESPONSE]")
   454  
   455      except WebSocketDisconnect:
   456          print("客户端断开连接")
   457          active_conversations.pop(client_id, None)
   458          usernames.pop(client_id, None)
   459      except Exception as e:
   460          print(f"客户端{client_id}发生错误：{e}")
   461          await websocket.send_text(f"发生错误: {e}")
   462          await websocket.close()
   463          active_conversations.pop(client_id, None)
   464          usernames.pop(client_id, None)
   465  
   466  @app.websocket("/ws/chat_with_claude/")
   467  async def chat_with_claude_websocket(websocket: WebSocket, world_id: Optional[int] = None):
   468      """WebSocket模拟流式聊天（支持多LLM）"""
   469      await websocket.accept()
   470      client_id = f"{websocket.client.host}:{websocket.client.port}"
   471  
   472      system_prompt = default_system_prompt
   473      greeting = "欢迎来到这片充满魔法的森林！(^_^) 我是森林精灵絮语，很高兴遇见你~"
   474  
   475      if world_id:
   476          with Session(engine) as session:
   477              world = session.get(World, world_id)
   478              if world:
   479                  if world.system_prompt:
   480                      system_prompt = world.system_prompt
   481                  if world.greeting_message:
   482                      greeting = world.greeting_message
   483  
   484      active_conversations[client_id] = [
   485          {"role": "assistant", "content": greeting}
   486      ]
   487      usernames[client_id] = current_username or "Unknown"
   488  
   489      with Session(engine) as session:
   490          user = session.exec(
   491              select(User).where(User.username == usernames[client_id])
   492          ).first()
   493          if not user:
   494              user = User(username="Unknown")
   495              session.add(user)
   496              session.commit()
   497              session.refresh(user)
   498  
   499      try:
   500          while True:
```

===== 第11页 =====

```
   501              data = await websocket.receive_json()
   502              user_message = data.get("content", "")
   503              llm_id = data.get("llm_id")  # 可选的LLM ID
   504              model = data.get("model")  # 可选的模型名称
   505              max_tokens = data.get("max_tokens", 2048)
   506  
   507              with Session(engine) as session:
   508                  chat = Chat(user_id=user.id, world_id=world_id, role="user", content=user_message)
   509                  session.add(chat)
   510                  session.commit()
   511  
   512              active_conversations[client_id].append(
   513                  {"role": "user", "content": user_message}
   514              )
   515  
   516              if not llm_manager.get_available_llms():
   517                  await websocket.send_text("抱歉，聊天服务暂时不可用")
   518                  await websocket.send_text("[END_OF_RESPONSE]")
   519                  continue
   520  
   521              # 收集完整响应
   522              full_response = ""
   523              for text in llm_manager.chat_stream(
   524                  messages=active_conversations[client_id],
   525                  system_prompt=system_prompt,
   526                  llm_id=llm_id,
   527                  model=model,
   528                  max_tokens=max_tokens
   529              ):
   530                  full_response += text
   531  
   532              # 模拟流式发送
   533              word_segments = list(jieba.cut(full_response))
   534              chunk_size = 6
   535              chunks = [
   536                  "".join(word_segments[i : i + chunk_size])
   537                  for i in range(0, len(word_segments), chunk_size)
   538              ]
   539              for chunk in chunks:
   540                  await websocket.send_text(chunk)
   541                  await asyncio.sleep(0.2)
   542  
   543              active_conversations[client_id].append(
   544                  {"role": "assistant", "content": full_response}
   545              )
   546              with Session(engine) as session:
   547                  chat = Chat(user_id=user.id, world_id=world_id, role="assistant", content=full_response)
   548                  session.add(chat)
   549                  session.commit()
   550  
```

===== 第12页 =====

```
   551              await websocket.send_text("[END_OF_RESPONSE]")
   552  
   553      except WebSocketDisconnect:
   554          print("客户端断开连接")
   555          active_conversations.pop(client_id, None)
   556          usernames.pop(client_id, None)
   557      except Exception as e:
   558          print(f"客户端{client_id}发生错误：{e}")
   559          await websocket.close()
   560          active_conversations.pop(client_id, None)
   561          usernames.pop(client_id, None)
   562  
   563  @app.get("/get_conversations/{username}")
   564  async def get_conversations(username: str):
   565      """获取用户对话记录"""
   566      with Session(engine) as session:
   567          user = session.exec(select(User).where(User.username == username)).first()
   568          if not user:
   569              raise HTTPException(status_code=404, detail="用户未找到")
   570  
   571          chats = session.exec(select(Chat).where(Chat.user_id == user.id)).all()
   572          return [
   573              {"role": chat.role, "content": chat.content, "timestamp": chat.timestamp}
   574              for chat in chats
   575          ]
   576  
   577  # ==================== 反馈路由（预留） ====================
   578  
   579  @app.post("/api/feedback")
   580  async def submit_feedback(user_id: int, request: FeedbackRequest):
   581      """提交反馈"""
   582      with Session(engine) as session:
   583          feedback = Feedback(
   584              user_id=user_id,
   585              feedback_type=request.feedback_type,
   586              title=request.title,
   587              content=request.content
   588          )
   589          session.add(feedback)
   590          session.commit()
   591  
   592          return {"message": "反馈提交成功"}
   593  
   594  @app.get("/api/feedback/{user_id}")
   595  async def get_user_feedback(user_id: int):
   596      """获取用户反馈"""
   597      with Session(engine) as session:
   598          feedbacks = session.exec(select(Feedback).where(Feedback.user_id == user_id)).all()
   599          return [
   600              {
```

===== 第13页 =====

```
   601                  "id": f.id,
   602                  "type": f.feedback_type,
   603                  "title": f.title,
   604                  "content": f.content,
   605                  "status": f.status,
   606                  "created_at": f.created_at
   607              }
   608              for f in feedbacks
   609          ]
   610  
   611  # ==================== 分享路由（预留） ====================
   612  
   613  @app.post("/api/share/{world_id}")
   614  async def share_world(world_id: int):
   615      """分享世界"""
   616      with Session(engine) as session:
   617          world = session.get(World, world_id)
   618          if not world:
   619              raise HTTPException(status_code=404, detail="世界未找到")
   620  
   621          # 检查是否已有分享
   622          existing = session.exec(
   623              select(WorldShare).where(WorldShare.world_id == world_id)
   624          ).first()
   625          if existing:
   626              return {"share_code": existing.share_code}
   627  
   628          share_code = uuid.uuid4().hex[:8]
   629          share = WorldShare(world_id=world_id, share_code=share_code)
   630          session.add(share)
   631  
   632          world.is_public = True
   633          session.add(world)
   634          session.commit()
   635  
   636          return {"share_code": share_code}
   637  
   638  @app.get("/api/share/world/{world_id}")
   639  async def get_share_by_world(world_id: int):
   640      """根据世界ID获取分享信息"""
   641      with Session(engine) as session:
   642          share = session.exec(
   643              select(WorldShare).where(WorldShare.world_id == world_id)
   644          ).first()
   645          if not share:
   646              raise HTTPException(status_code=404, detail="未找到分享")
   647  
   648          return {
   649              "share_code": share.share_code,
   650              "view_count": share.view_count,
```

===== 第14页 =====

```
   651              "created_at": share.created_at
   652          }
   653  
   654  @app.get("/api/share/{share_code}")
   655  async def get_shared_world(share_code: str):
   656      """获取分享的世界"""
   657      with Session(engine) as session:
   658          share = session.exec(
   659              select(WorldShare).where(WorldShare.share_code == share_code)
   660          ).first()
   661          if not share:
   662              raise HTTPException(status_code=404, detail="分享链接无效")
   663  
   664          # 增加访问计数
   665          share.view_count += 1
   666          session.add(share)
   667  
   668          world = session.get(World, share.world_id)
   669          session.commit()
   670  
   671          return {
   672              "world_id": world.id,
   673              "name": world.name,
   674              "sky_texture": world.sky_texture,
   675              "fairy_model": world.fairy_model,
   676              "background_music": world.background_music,
   677              "greeting_message": world.greeting_message,
   678              "view_count": share.view_count
   679          }
   680  
   681  # ==================== 管理路由 ====================
   682  
   683  @app.get("/api/admin/db-info")
   684  async def get_database_info():
   685      """获取数据库信息"""
   686      return get_db_info()
   687  
   688  @app.post("/api/admin/db-reset")
   689  async def reset_database(confirm: bool = False):
   690      """重置数据库（危险操作）"""
   691      if not confirm:
   692          raise HTTPException(status_code=400, detail="请确认此操作，设置confirm=true")
   693  
   694      reset_db()
   695      return {"message": "数据库已重置"}
   696  
   697  @app.post("/api/admin/db-migrate")
   698  async def migrate_database():
   699      """数据库迁移（重建表结构）"""
   700      init_db()
```

===== 第15页 =====

```
   701      return {"message": "数据库迁移完成"}
   702  
   703  @app.get("/api/admin/upload-records")
   704  async def get_all_upload_records():
   705      """获取所有上传记录"""
   706      with Session(engine) as session:
   707          records = session.exec(select(UploadRecord)).all()
   708          return [
   709              {
   710                  "id": r.id,
   711                  "user_id": r.user_id,
   712                  "world_id": r.world_id,
   713                  "file_type": r.file_type,
   714                  "original_filename": r.original_filename,
   715                  "file_path": r.file_path,
   716                  "file_size": r.file_size,
   717                  "uploaded_at": r.uploaded_at
   718              }
   719              for r in records
   720          ]
   721  
   722  # ==================== 预留路由占位 ====================
   723  
   724  @app.get("/api/my-worlds")
   725  async def my_worlds_placeholder():
   726      """我的世界 - 功能预留"""
   727      return {"message": "此功能正在开发中", "status": "placeholder"}
   728  
   729  @app.get("/api/explore")
   730  async def explore_worlds_placeholder():
   731      """探索世界 - 功能预留"""
   732      return {"message": "此功能正在开发中", "status": "placeholder"}
   733  
   734  @app.post("/api/auth/forgot-password")
   735  async def forgot_password_placeholder():
   736      """忘记密码 - 功能预留（当前系统无密码）"""
   737      return {"message": "当前系统不需要密码", "status": "placeholder"}
   738  
   739  # ==================== LLM路由 ====================
   740  
   741  @app.get("/api/llms")
   742  async def get_available_llms():
   743      """获取可用的LLM列表"""
   744      return llm_manager.get_available_llms()
   745  
   746  @app.get("/api/llms/default")
   747  async def get_default_llm():
   748      """获取默认LLM"""
   749      config = llm_manager.get_config()
   750      if not config:
```

===== 第16页 =====

```
   751          raise HTTPException(status_code=404, detail="没有可用的LLM")
   752      return {
   753          'id': config.id,
   754          'name': config.name,
   755          'default_model': config.default_model,
   756          'models': config.models
   757      }
   758  
   759  @app.post("/api/llms/test/{llm_id}")
   760  async def test_llm(llm_id: str):
   761      """测试指定LLM是否可用"""
   762      config = llm_manager.get_config(llm_id)
   763      if not config:
   764          raise HTTPException(status_code=404, detail=f"LLM {llm_id} 未找到")
   765  
   766      try:
   767          # 发送简单测试消息
   768          response_text = ""
   769          for text in llm_manager.chat_stream(
   770              messages=[{"role": "user", "content": "请回复'你好'两个字"}],
   771              system_prompt="你是一个测试助手，请简短回复。",
   772              llm_id=llm_id,
   773              max_tokens=50
   774          ):
   775              response_text += text
   776  
   777          return {
   778              "status": "success",
   779              "llm_id": llm_id,
   780              "llm_name": config.name,
   781              "response": response_text[:100]  # 截取前100字符
   782          }
   783      except Exception as e:
   784          return {
   785              "status": "error",
   786              "llm_id": llm_id,
   787              "llm_name": config.name,
   788              "error": str(e)
   789          }
   790  
   791  # ==================== 健康检查 ====================
   792  
   793  @app.get("/health")
   794  async def health_check():
   795      """健康检查"""
   796      available_llms = llm_manager.get_available_llms()
   797      return {
   798          "status": "healthy",
   799          "database": get_db_info(),
   800          "llm_available": len(available_llms) > 0,
```

===== 第17页 =====

```
   801          "available_llms": [llm['id'] for llm in available_llms],
   802          "default_llm": llm_manager.default_llm_id
   803      }
   804  
   805  
   806  if __name__ == "__main__":
   807      import uvicorn
   808      uvicorn.run(app, host="0.0.0.0", port=8000)
```

### 文件: llm_manager.py

**路径:** ../backend/llm_manager.py  
**说明:** LLM管理器，支持多AI模型  
**行数:** 216行

```
     1  """
     2  LLM管理模块 - 支持多LLM配置
     3  """
     4  import os
     5  import yaml
     6  from typing import Dict, List, Optional, Generator
     7  from dataclasses import dataclass
     8  from anthropic import Anthropic, DefaultHttpxClient
     9  from openai import OpenAI
    10  import httpx
    11  
    12  
    13  @dataclass
    14  class LLMConfig:
    15      """LLM配置数据类"""
    16      id: str
    17      name: str
    18      provider: str
    19      api_key: str
    20      base_url: Optional[str]
    21      proxy: Optional[str]
    22      default_model: str
    23      models: List[str]
    24      enabled: bool
    25  
    26  
    27  class LLMManager:
    28      """LLM客户端管理器"""
    29  
    30      def __init__(self, config_path: str = "llm_config.yaml"):
    31          self.config_path = config_path
    32          self.llm_configs: Dict[str, LLMConfig] = {}
    33          self.clients: Dict[str, object] = {}
    34          self.default_llm_id: Optional[str] = None
    35          self._load_config()
    36  
    37      def _load_config(self):
    38          """加载yaml配置文件"""
    39          if not os.path.exists(self.config_path):
    40              print(f"警告: LLM配置文件 {self.config_path} 不存在")
    41              return
    42  
```

===== 第18页 =====

```
    43          with open(self.config_path, 'r', encoding='utf-8') as f:
    44              config = yaml.safe_load(f)
    45  
    46          llms = config.get('llms', [])
    47          for i, llm_data in enumerate(llms):
    48              if not llm_data.get('enabled', True):
    49                  continue
    50  
    51              # 处理API密钥
    52              api_key = llm_data.get('api_key', '')
    53              if llm_data.get('api_key_env'):
    54                  api_key = os.getenv(llm_data['api_key_env'], '')
    55  
    56              if not api_key:
    57                  print(f"警告: LLM {llm_data.get('id')} 缺少API密钥，跳过")
    58                  continue
    59  
    60              llm_config = LLMConfig(
    61                  id=llm_data['id'],
    62                  name=llm_data['name'],
    63                  provider=llm_data['provider'],
    64                  api_key=api_key,
    65                  base_url=llm_data.get('base_url'),
    66                  proxy=llm_data.get('proxy'),
    67                  default_model=llm_data.get('default_model', ''),
    68                  models=llm_data.get('models', []),
    69                  enabled=True
    70              )
    71  
    72              self.llm_configs[llm_config.id] = llm_config
    73              self._init_client(llm_config)
    74  
    75              # 第一个有效的LLM为默认
    76              if i == 0 or self.default_llm_id is None:
    77                  self.default_llm_id = llm_config.id
    78  
    79          print(f"已加载 {len(self.llm_configs)} 个LLM配置，默认: {self.default_llm_id}")
    80  
    81      def _init_client(self, config: LLMConfig):
    82          """初始化LLM客户端"""
    83          try:
    84              if config.provider == 'anthropic':
    85                  if config.proxy:
    86                      try:
    87                          client = Anthropic(
    88                              http_client=DefaultHttpxClient(
    89                                  proxy=config.proxy,
    90                                  transport=httpx.HTTPTransport(local_address="0.0.0.0"),
    91                              ),
    92                              api_key=config.api_key,
```

===== 第19页 =====

```
    93                          )
    94                      except TypeError:
    95                          client = Anthropic(api_key=config.api_key)
    96                  else:
    97                      client = Anthropic(api_key=config.api_key)
    98                  self.clients[config.id] = client
    99                  print(f"已初始化Anthropic客户端: {config.name}")
   100  
   101              elif config.provider == 'openai_compatible':
   102                  client = OpenAI(
   103                      api_key=config.api_key,
   104                      base_url=config.base_url
   105                  )
   106                  self.clients[config.id] = client
   107                  print(f"已初始化OpenAI兼容客户端: {config.name}")
   108  
   109          except Exception as e:
   110              print(f"初始化LLM客户端失败 {config.id}: {e}")
   111  
   112      def get_available_llms(self) -> List[Dict]:
   113          """获取可用的LLM列表"""
   114          result = []
   115          for llm_id, config in self.llm_configs.items():
   116              if llm_id in self.clients:
   117                  result.append({
   118                      'id': config.id,
   119                      'name': config.name,
   120                      'provider': config.provider,
   121                      'default_model': config.default_model,
   122                      'models': config.models,
   123                      'is_default': llm_id == self.default_llm_id
   124                  })
   125          return result
   126  
   127      def get_client(self, llm_id: Optional[str] = None):
   128          """获取LLM客户端"""
   129          if llm_id is None:
   130              llm_id = self.default_llm_id
   131          return self.clients.get(llm_id)
   132  
   133      def get_config(self, llm_id: Optional[str] = None) -> Optional[LLMConfig]:
   134          """获取LLM配置"""
   135          if llm_id is None:
   136              llm_id = self.default_llm_id
   137          return self.llm_configs.get(llm_id)
   138  
   139      def chat_stream(
   140          self,
   141          messages: List[Dict],
   142          system_prompt: str,
```

===== 第20页 =====

```
   143          llm_id: Optional[str] = None,
   144          model: Optional[str] = None,
   145          max_tokens: int = 2048
   146      ) -> Generator[str, None, None]:
   147          """
   148          统一的流式聊天接口
   149          返回文本流生成器
   150          """
   151          if llm_id is None:
   152              llm_id = self.default_llm_id
   153  
   154          client = self.clients.get(llm_id)
   155          config = self.llm_configs.get(llm_id)
   156  
   157          if not client or not config:
   158              yield "错误: LLM客户端未初始化"
   159              return
   160  
   161          if model is None:
   162              model = config.default_model
   163  
   164          try:
   165              if config.provider == 'anthropic':
   166                  yield from self._anthropic_stream(client, messages, system_prompt, model, max_tokens)
   167              elif config.provider == 'openai_compatible':
   168                  yield from self._openai_stream(client, messages, system_prompt, model, max_tokens)
   169          except Exception as e:
   170              yield f"错误: {str(e)}"
   171  
   172      def _anthropic_stream(
   173          self,
   174          client: Anthropic,
   175          messages: List[Dict],
   176          system_prompt: str,
   177          model: str,
   178          max_tokens: int
   179      ) -> Generator[str, None, None]:
   180          """Anthropic流式聊天"""
   181          with client.messages.stream(
   182              model=model,
   183              max_tokens=max_tokens,
   184              system=system_prompt,
   185              messages=messages,
   186          ) as stream:
   187              for text in stream.text_stream:
   188                  yield text
   189  
   190      def _openai_stream(
   191          self,
   192          client: OpenAI,
```

===== 第21页 =====

```
   193          messages: List[Dict],
   194          system_prompt: str,
   195          model: str,
   196          max_tokens: int
   197      ) -> Generator[str, None, None]:
   198          """OpenAI兼容流式聊天"""
   199          # 构建消息列表，添加system消息
   200          full_messages = [{"role": "system", "content": system_prompt}]
   201          full_messages.extend(messages)
   202  
   203          response = client.chat.completions.create(
   204              model=model,
   205              messages=full_messages,
   206              max_tokens=max_tokens,
   207              stream=True
   208          )
   209  
   210          for chunk in response:
   211              if chunk.choices[0].delta.content:
   212                  yield chunk.choices[0].delta.content
   213  
   214  
   215  # 全局LLM管理器实例
   216  llm_manager = LLMManager()
```

### 文件: models.py

**路径:** ../backend/models.py  
**说明:** 数据库模型定义  
**行数:** 119行

```
     1  """
     2  数据库模型定义
     3  """
     4  from datetime import datetime
     5  from typing import Optional, List
     6  from sqlmodel import Field, SQLModel, Relationship
     7  from enum import Enum
     8  
     9  
    10  class FeedbackType(str, Enum):
    11      """反馈类型"""
    12      FEATURE_REQUEST = "feature_request"  # 期待的交互建议
    13      BUG_REPORT = "bug_report"  # bug反馈
    14  
    15  
    16  # ==================== 用户模型 ====================
    17  
    18  class User(SQLModel, table=True):
    19      """用户表"""
    20      id: Optional[int] = Field(default=None, primary_key=True)
    21      username: str = Field(unique=True, index=True)
    22      created_at: datetime = Field(default_factory=datetime.utcnow)
    23  
    24      # 关系
    25      worlds: List["World"] = Relationship(back_populates="user")
    26      chats: List["Chat"] = Relationship(back_populates="user")
```

===== 第22页 =====

```
    27      feedbacks: List["Feedback"] = Relationship(back_populates="user")
    28  
    29  
    30  # ==================== 世界配置模型 ====================
    31  
    32  class World(SQLModel, table=True):
    33      """用户创建的世界配置"""
    34      id: Optional[int] = Field(default=None, primary_key=True)
    35      user_id: int = Field(foreign_key="user.id", index=True)
    36      name: str = Field(default="我的世界")
    37  
    38      # 自定义资源路径（相对于uploads目录）
    39      sky_texture: Optional[str] = None  # 天空贴图
    40      fairy_model: Optional[str] = None  # 精灵模型
    41      background_music: Optional[str] = None  # 背景音乐
    42  
    43      # 精灵配置
    44      system_prompt: Optional[str] = None  # 系统提示词
    45      greeting_message: Optional[str] = None  # 初次见面语
    46  
    47      # 元数据
    48      is_public: bool = Field(default=False)  # 是否公开分享
    49      created_at: datetime = Field(default_factory=datetime.utcnow)
    50      updated_at: datetime = Field(default_factory=datetime.utcnow)
    51  
    52      # 关系
    53      user: Optional[User] = Relationship(back_populates="worlds")
    54  
    55  
    56  # ==================== 聊天记录模型 ====================
    57  
    58  class Chat(SQLModel, table=True):
    59      """聊天记录表"""
    60      id: Optional[int] = Field(default=None, primary_key=True)
    61      user_id: int = Field(foreign_key="user.id", index=True)
    62      world_id: Optional[int] = Field(default=None, foreign_key="world.id", index=True)
    63      role: str  # "user" 或 "assistant"
    64      content: str
    65      timestamp: datetime = Field(default_factory=datetime.utcnow)
    66  
    67      # 关系
    68      user: Optional[User] = Relationship(back_populates="chats")
    69  
    70  
    71  # ==================== 上传记录模型 ====================
    72  
    73  class UploadRecord(SQLModel, table=True):
    74      """文件上传记录"""
    75      id: Optional[int] = Field(default=None, primary_key=True)
    76      user_id: int = Field(foreign_key="user.id", index=True)
```

===== 第23页 =====

```
    77      world_id: Optional[int] = Field(default=None, foreign_key="world.id", index=True)
    78  
    79      file_type: str  # "sky_texture", "fairy_model", "background_music", "system_prompt"
    80      original_filename: str
    81      stored_filename: str
    82      file_path: str
    83      file_size: int
    84  
    85      uploaded_at: datetime = Field(default_factory=datetime.utcnow)
    86  
    87  
    88  # ==================== 反馈模型 ====================
    89  
    90  class Feedback(SQLModel, table=True):
    91      """用户反馈表"""
    92      id: Optional[int] = Field(default=None, primary_key=True)
    93      user_id: int = Field(foreign_key="user.id", index=True)
    94  
    95      feedback_type: str  # FeedbackType
    96      title: str
    97      content: str
    98  
    99      # 状态
   100      status: str = Field(default="pending")  # pending, reviewed, resolved
   101  
   102      created_at: datetime = Field(default_factory=datetime.utcnow)
   103  
   104      # 关系
   105      user: Optional[User] = Relationship(back_populates="feedbacks")
   106  
   107  
   108  # ==================== 世界分享模型 ====================
   109  
   110  class WorldShare(SQLModel, table=True):
   111      """世界分享记录"""
   112      id: Optional[int] = Field(default=None, primary_key=True)
   113      world_id: int = Field(foreign_key="world.id", index=True)
   114      share_code: str = Field(unique=True, index=True)  # 分享码
   115  
   116      view_count: int = Field(default=0)
   117  
   118      created_at: datetime = Field(default_factory=datetime.utcnow)
   119      expires_at: Optional[datetime] = None  # 过期时间，null表示永不过期
```

### 文件: config.py

**路径:** ../backend/config.py  
**说明:** 配置模块  
**行数:** 87行

```
     1  """
     2  数据库和应用配置模块
     3  支持SQLite（默认）和MySQL
     4  """
     5  import os
     6  from dotenv import load_dotenv
     7  from sqlmodel import create_engine, SQLModel
```

===== 第24页 =====

```
     8  from typing import Optional
     9  
    10  load_dotenv()
    11  
    12  # 数据库配置
    13  # 默认使用SQLite，可通过环境变量DATABASE_URL配置MySQL
    14  # MySQL示例: mysql+pymysql://user:password@localhost:3306/dbname
    15  # SQLite示例: sqlite:///./chat_database.db
    16  DATABASE_URL = os.getenv("DATABASE_URL", "sqlite:///./chat_database.db")
    17  
    18  # 根据数据库类型配置引擎参数
    19  def get_engine_args():
    20      """获取数据库引擎参数"""
    21      if DATABASE_URL.startswith("sqlite"):
    22          return {"connect_args": {"check_same_thread": False}}
    23      return {}
    24  
    25  # 创建数据库引擎
    26  engine = create_engine(DATABASE_URL, **get_engine_args())
    27  
    28  # 应用配置
    29  class Settings:
    30      # 环境配置
    31      ENV: str = os.getenv("ENV", "development")
    32  
    33      # 文件上传配置
    34      UPLOAD_DIR: str = os.getenv("UPLOAD_DIR", "./uploads")
    35      MAX_UPLOAD_SIZE: int = int(os.getenv("MAX_UPLOAD_SIZE", 50 * 1024 * 1024))  # 50MB
    36  
    37      # 允许的文件类型
    38      ALLOWED_IMAGE_TYPES = {".jpg", ".jpeg", ".png", ".webp", ".hdr"}
    39      ALLOWED_MODEL_TYPES = {".gltf", ".glb", ".bin"}
    40      ALLOWED_AUDIO_TYPES = {".mp3", ".wav", ".ogg"}
    41      ALLOWED_TEXT_TYPES = {".md", ".txt"}
    42  
    43      # API密钥
    44      ANTHROPIC_API_KEY: Optional[str] = os.getenv("ANTHROPIC_API_KEY")
    45  
    46      # 代理配置（开发环境）
    47      PROXY_URL: str = os.getenv("PROXY_URL", "http://127.0.0.1:10809")
    48  
    49  settings = Settings()
    50  
    51  def init_db():
    52      """初始化数据库表"""
    53      SQLModel.metadata.create_all(engine)
    54      # 运行迁移检查
    55      migrate_db()
    56  
    57  def migrate_db():
```

===== 第25页 =====

```
    58      """检查并添加缺失的数据库列"""
    59      from sqlalchemy import text, inspect
    60  
    61      inspector = inspect(engine)
    62  
    63      # 检查chat表是否存在world_id列
    64      if 'chat' in inspector.get_table_names():
    65          columns = [col['name'] for col in inspector.get_columns('chat')]
    66          if 'world_id' not in columns:
    67              print("正在为chat表添加world_id列...")
    68              with engine.connect() as conn:
    69                  if DATABASE_URL.startswith("sqlite"):
    70                      conn.execute(text("ALTER TABLE chat ADD COLUMN world_id INTEGER"))
    71                  else:
    72                      conn.execute(text("ALTER TABLE chat ADD COLUMN world_id INTEGER"))
    73                  conn.commit()
    74              print("world_id列添加成功")
    75  
    76  def reset_db():
    77      """重置数据库（删除所有表并重新创建）"""
    78      SQLModel.metadata.drop_all(engine)
    79      SQLModel.metadata.create_all(engine)
    80  
    81  def get_db_info():
    82      """获取数据库信息"""
    83      db_type = "SQLite" if DATABASE_URL.startswith("sqlite") else "MySQL"
    84      return {
    85          "type": db_type,
    86          "url": DATABASE_URL if db_type == "SQLite" else DATABASE_URL.split("@")[-1] if "@" in DATABASE_URL else DATABASE_URL
    87      }
```

### 文件: world-config.js

**路径:** ../frontend/world-config.js  
**说明:** 世界配置模块，文件上传和分享功能  
**行数:** 849行

```
     1  /**
     2   * 世界配置模块 - 用户可以上传自定义资源
     3   */
     4  
     5  class WorldConfig {
     6      constructor() {
     7          this.apiBase = this.getApiBase();
     8          this.currentWorldId = null;
     9          this.currentUserId = null;
    10          this.availableLlms = [];
    11      }
    12  
    13      getApiBase() {
    14          if (window.location.hostname === "127.0.0.1" || window.location.hostname === "localhost") {
    15              return "http://127.0.0.1:8000";
    16          }
    17          return "https://api.psyventures.cn";
    18      }
    19  
    20      async init() {
```

===== 第26页 =====

```
    21          // 加载可用的LLM列表
    22          await this.loadAvailableLlms();
    23  
    24          // 从localStorage获取用户信息
    25          const username = localStorage.getItem("username");
    26          if (username) {
    27              await this.loadUserData(username);
    28          } else {
    29              // 没有登录时，显示默认场景
    30              this.showDefaultScene();
    31          }
    32          this.setupEventListeners();
    33      }
    34  
    35      showDefaultScene() {
    36          // 等待场景加载完成后显示默认元素
    37          const scene = document.querySelector('a-scene');
    38          const showDefaults = () => {
    39              const sky = document.querySelector('#sky');
    40              const fairy = document.querySelector('#fairy');
    41              if (sky) sky.setAttribute('visible', 'true');
    42              if (fairy) {
    43                  // 加载默认模型
    44                  fairy.setAttribute('gltf-model', '#fairyModel');
    45                  fairy.addEventListener('model-loaded', () => {
    46                      fairy.setAttribute('visible', 'true');
    47                  }, { once: true });
    48              }
    49          };
    50  
    51          if (scene && scene.hasLoaded) {
    52              showDefaults();
    53          } else if (scene) {
    54              scene.addEventListener('loaded', showDefaults, { once: true });
    55          }
    56      }
    57  
    58      async loadAvailableLlms() {
    59          try {
    60              const response = await fetch(`${this.apiBase}/api/llms`);
    61              if (response.ok) {
    62                  this.availableLlms = await response.json();
    63                  this.renderLlmSelector();
    64                  console.log("可用LLM列表:", this.availableLlms);
    65              }
    66          } catch (error) {
    67              console.error("加载LLM列表失败:", error);
    68          }
    69      }
    70  
```

===== 第27页 =====

```
    71      renderLlmSelector() {
    72          const container = document.getElementById('llm-selector-container');
    73          if (!container || this.availableLlms.length === 0) return;
    74  
    75          const select = document.getElementById('llm-selector');
    76          if (!select) return;
    77  
    78          // 清空现有选项
    79          select.innerHTML = '';
    80  
    81          // 添加LLM选项
    82          this.availableLlms.forEach(llm => {
    83              const option = document.createElement('option');
    84              option.value = llm.id;
    85              option.textContent = llm.name + (llm.is_default ? ' (默认)' : '');
    86              if (llm.is_default) {
    87                  option.selected = true;
    88              }
    89              select.appendChild(option);
    90          });
    91  
    92          // 监听选择变化
    93          select.addEventListener('change', (e) => {
    94              const selectedId = e.target.value;
    95              // 更新全局LLM ID
    96              if (typeof currentLlmId !== 'undefined') {
    97                  window.currentLlmId = selectedId;
    98              }
    99              console.log("已选择LLM:", selectedId);
   100              // 重置测试状态
   101              const statusEl = document.getElementById('llm-test-status');
   102              if (statusEl) statusEl.textContent = '';
   103          });
   104  
   105          // 绑定测试按钮事件
   106          const testBtn = document.getElementById('test-llm-btn');
   107          if (testBtn) {
   108              testBtn.addEventListener('click', () => this.testSelectedLlm());
   109          }
   110  
   111          // 显示容器
   112          container.style.display = 'block';
   113      }
   114  
   115      async testSelectedLlm() {
   116          const select = document.getElementById('llm-selector');
   117          const statusEl = document.getElementById('llm-test-status');
   118          const testBtn = document.getElementById('test-llm-btn');
   119  
   120          if (!select || !statusEl) return;
```

===== 第28页 =====

```
   121  
   122          const llmId = select.value;
   123          statusEl.textContent = '测试中...';
   124          statusEl.style.color = '#7ec8e3';
   125          if (testBtn) testBtn.disabled = true;
   126  
   127          try {
   128              const response = await fetch(`${this.apiBase}/api/llms/test/${llmId}`, {
   129                  method: 'POST'
   130              });
   131              const result = await response.json();
   132  
   133              if (result.status === 'success') {
   134                  statusEl.textContent = `✓ ${result.llm_name} 连接正常`;
   135                  statusEl.style.color = '#4ade80';
   136              } else {
   137                  statusEl.textContent = `✗ 连接失败: ${result.error}`;
   138                  statusEl.style.color = '#f87171';
   139              }
   140          } catch (error) {
   141              statusEl.textContent = `✗ 测试失败: ${error.message}`;
   142              statusEl.style.color = '#f87171';
   143          } finally {
   144              if (testBtn) testBtn.disabled = false;
   145          }
   146      }
   147  
   148      async loadUserData(username) {
   149          try {
   150              const response = await fetch(`${this.apiBase}/api/users/${username}`);
   151              if (response.ok) {
   152                  const user = await response.json();
   153                  this.currentUserId = user.id;
   154                  await this.loadUserWorlds(user.id);
   155              } else {
   156                  // 用户不存在，显示默认场景
   157                  this.showDefaultScene();
   158              }
   159          } catch (error) {
   160              console.error("加载用户数据失败:", error);
   161              // 加载失败，显示默认场景
   162              this.showDefaultScene();
   163          }
   164      }
   165  
   166      async loadUserWorlds(userId) {
   167          try {
   168              const response = await fetch(`${this.apiBase}/api/worlds/${userId}`);
   169              if (response.ok) {
   170                  const worlds = await response.json();
```

===== 第29页 =====

```
   171                  if (worlds.length > 0) {
   172                      this.currentWorldId = worlds[0].id;
   173                      window.currentWorldId = worlds[0].id;  // 暴露给chat.js使用
   174                      this.applyWorldConfig(worlds[0]);
   175                  } else {
   176                      // 用户没有世界，显示默认场景
   177                      this.showDefaultScene();
   178                  }
   179              } else {
   180                  // 请求失败，显示默认场景
   181                  this.showDefaultScene();
   182              }
   183          } catch (error) {
   184              console.error("加载世界配置失败:", error);
   185              // 加载失败，显示默认场景
   186              this.showDefaultScene();
   187          }
   188      }
   189  
   190      applyWorldConfig(world) {
   191          // 等待场景加载完成后再应用配置
   192          const scene = document.querySelector('a-scene');
   193          if (scene && scene.hasLoaded) {
   194              this._applyWorldConfigInternal(world);
   195          } else if (scene) {
   196              scene.addEventListener('loaded', () => {
   197                  this._applyWorldConfigInternal(world);
   198              }, { once: true });
   199          }
   200      }
   201  
   202      _applyWorldConfigInternal(world) {
   203          // 应用天空贴图
   204          if (world.sky_texture) {
   205              this.updateSkyTexture(`${this.apiBase}/uploads/${world.sky_texture}`);
   206          } else {
   207              // 没有自定义，显示默认
   208              const sky = document.querySelector('#sky');
   209              if (sky) sky.setAttribute('visible', 'true');
   210          }
   211  
   212          // 应用身外化身模型
   213          if (world.fairy_model) {
   214              this.updateFairyModel(`${this.apiBase}/uploads/${world.fairy_model}`);
   215          } else {
   216              // 没有自定义，加载并显示默认模型
   217              const fairy = document.querySelector('#fairy');
   218              if (fairy) {
   219                  fairy.setAttribute('gltf-model', '#fairyModel');
   220                  fairy.addEventListener('model-loaded', () => {
```

===== 第30页 =====

```
   221                      fairy.setAttribute('visible', 'true');
   222                  }, { once: true });
   223              }
   224          }
   225  
   226          // 应用背景音乐
   227          if (world.background_music) {
   228              const musicEl = document.querySelector('#background-music');
   229              if (musicEl) {
   230                  musicEl.src = `${this.apiBase}/uploads/${world.background_music}`;
   231              }
   232          }
   233  
   234          // 应用开场白
   235          if (world.greeting_message) {
   236              window.customGreeting = world.greeting_message;
   237          }
   238      }
   239  
   240      setupEventListeners() {
   241          // 设置面板打开按钮
   242          const configBtn = document.getElementById('config-button');
   243          if (configBtn) {
   244              configBtn.addEventListener('click', () => this.openConfigPanel());
   245          }
   246  
   247          // 设置面板关闭按钮
   248          const closeBtn = document.getElementById('close-config');
   249          if (closeBtn) {
   250              closeBtn.addEventListener('click', () => this.closeConfigPanel());
   251          }
   252  
   253          // 文件上传事件
   254          this.setupUploadListeners();
   255      }
   256  
   257      setupUploadListeners() {
   258          // 天空贴图上传
   259          const skyInput = document.getElementById('sky-texture-input');
   260          if (skyInput) {
   261              skyInput.addEventListener('change', (e) => this.uploadFile(e, 'sky-texture'));
   262          }
   263  
   264          // 精灵模型上传
   265          const fairyInput = document.getElementById('fairy-model-input');
   266          if (fairyInput) {
   267              fairyInput.addEventListener('change', (e) => this.uploadFile(e, 'fairy-model'));
   268          }
   269  
   270          // 背景音乐上传
```

---

## 后30页源代码 (第1501-3000行)

===== 第31页 =====

```
   271          const musicInput = document.getElementById('music-input');
   272          if (musicInput) {
   273              musicInput.addEventListener('change', (e) => this.uploadFile(e, 'background-music'));
   274          }
   275  
   276          // 系统提示词上传
   277          const promptInput = document.getElementById('prompt-input');
   278          if (promptInput) {
   279              promptInput.addEventListener('change', (e) => this.uploadFile(e, 'system-prompt'));
   280          }
   281  
   282          // 文本表单保存
   283          const saveGreetingBtn = document.getElementById('save-greeting');
   284          if (saveGreetingBtn) {
   285              saveGreetingBtn.addEventListener('click', () => this.saveGreeting());
   286          }
   287  
   288          const savePromptTextBtn = document.getElementById('save-prompt-text');
   289          if (savePromptTextBtn) {
   290              savePromptTextBtn.addEventListener('click', () => this.savePromptText());
   291          }
   292      }
   293  
   294      async uploadFile(event, fileType) {
   295          const file = event.target.files[0];
   296          if (!file) return;
   297  
   298          if (!this.currentWorldId) {
   299              alert("请先登录");
   300              return;
   301          }
   302  
   303          const formData = new FormData();
   304          formData.append('file', file);
   305  
   306          const statusEl = document.getElementById(`${fileType}-status`);
   307          if (statusEl) statusEl.textContent = "上传中...";
   308  
   309          try {
   310              const response = await fetch(
   311                  `${this.apiBase}/api/upload/${fileType}/${this.currentWorldId}`,
   312                  {
   313                      method: 'POST',
   314                      body: formData
   315                  }
   316              );
   317  
   318              if (response.ok) {
   319                  const result = await response.json();
   320                  if (statusEl) statusEl.textContent = "上传成功!";
```

===== 第32页 =====

```
   321  
   322                  // 立即应用更改
   323                  this.applyUploadedFile(fileType, result.path);
   324  
   325                  setTimeout(() => {
   326                      if (statusEl) statusEl.textContent = "";
   327                  }, 3000);
   328              } else {
   329                  const error = await response.json();
   330                  if (statusEl) statusEl.textContent = `错误: ${error.detail}`;
   331              }
   332          } catch (error) {
   333              console.error("上传失败:", error);
   334              if (statusEl) statusEl.textContent = "上传失败";
   335          }
   336      }
   337  
   338      applyUploadedFile(fileType, path) {
   339          const fullPath = `${this.apiBase}${path}`;
   340          console.log(`应用上传文件: ${fileType} -> ${fullPath}`);
   341  
   342          switch (fileType) {
   343              case 'sky-texture':
   344                  this.updateSkyTexture(fullPath);
   345                  break;
   346  
   347              case 'fairy-model':
   348                  console.log('准备更新身外化身模型...');
   349                  this.updateFairyModel(fullPath);
   350                  break;
   351  
   352              case 'background-music':
   353                  console.log('准备更新背景音乐...');
   354                  const music = document.querySelector('#background-music');
   355                  if (music) {
   356                      music.src = fullPath;
   357                      music.load();
   358                      console.log('背景音乐已更新:', fullPath);
   359                  }
   360                  break;
   361          }
   362      }
   363  
   364      updateSkyTexture(fullPath) {
   365          const sky = document.querySelector('#sky');
   366          if (!sky) return;
   367  
   368          // 立即隐藏当前sky，避免闪烁
   369          sky.setAttribute('visible', 'false');
   370  
```

===== 第33页 =====

```
   371          // 创建新的图片元素来预加载
   372          const img = new Image();
   373          img.crossOrigin = "anonymous";
   374          img.onload = () => {
   375              // 图片加载完成后，直接设置天空盒的src为完整URL
   376              sky.setAttribute('src', fullPath);
   377              // 短暂延迟后显示，确保A-Frame处理完成
   378              setTimeout(() => {
   379                  sky.setAttribute('visible', 'true');
   380                  console.log('天空贴图已更新');
   381              }, 100);
   382          };
   383          img.onerror = (e) => {
   384              console.error('天空贴图加载失败:', e);
   385              // 加载失败时恢复显示
   386              sky.setAttribute('visible', 'true');
   387          };
   388          img.src = fullPath;
   389      }
   390  
   391      updateFairyModel(fullPath) {
   392          const fairy = document.querySelector('#fairy');
   393          if (!fairy) return;
   394  
   395          console.log('开始更新身外化身模型:', fullPath);
   396  
   397          // 立即隐藏当前模型，避免闪烁
   398          fairy.setAttribute('visible', 'false');
   399  
   400          // 先移除旧的gltf-model属性，再设置新的
   401          fairy.removeAttribute('gltf-model');
   402  
   403          // 短暂延迟后设置新模型，确保旧模型被清理
   404          setTimeout(() => {
   405              fairy.setAttribute('gltf-model', fullPath);
   406  
   407              // 监听模型加载完成事件
   408              const onLoaded = () => {
   409                  fairy.setAttribute('visible', 'true');
   410                  console.log('身外化身模型已更新');
   411                  fairy.removeEventListener('model-loaded', onLoaded);
   412              };
   413  
   414              const onError = (e) => {
   415                  console.error('身外化身模型加载失败:', e.detail);
   416                  // 加载失败时恢复显示默认模型
   417                  fairy.setAttribute('gltf-model', '#fairyModel');
   418                  fairy.setAttribute('visible', 'true');
   419                  fairy.removeEventListener('model-error', onError);
   420              };
```

===== 第34页 =====

```
   421  
   422              fairy.addEventListener('model-loaded', onLoaded);
   423              fairy.addEventListener('model-error', onError);
   424          }, 100);
   425      }
   426  
   427      async saveGreeting() {
   428          const greetingText = document.getElementById('greeting-text');
   429          if (!greetingText || !this.currentWorldId) return;
   430  
   431          try {
   432              const response = await fetch(
   433                  `${this.apiBase}/api/worlds/${this.currentWorldId}`,
   434                  {
   435                      method: 'PUT',
   436                      headers: { 'Content-Type': 'application/json' },
   437                      body: JSON.stringify({ greeting_message: greetingText.value })
   438                  }
   439              );
   440  
   441              if (response.ok) {
   442                  alert("初次见面语已保存!");
   443                  window.customGreeting = greetingText.value;
   444                  // 更新对话框中的开场白
   445                  if (window.updateOpeningDialogue) {
   446                      window.updateOpeningDialogue(greetingText.value);
   447                  }
   448              }
   449          } catch (error) {
   450              console.error("保存失败:", error);
   451              alert("保存失败");
   452          }
   453      }
   454  
   455      async savePromptText() {
   456          const promptText = document.getElementById('prompt-text');
   457          if (!promptText || !this.currentWorldId) return;
   458  
   459          try {
   460              const response = await fetch(
   461                  `${this.apiBase}/api/worlds/${this.currentWorldId}`,
   462                  {
   463                      method: 'PUT',
   464                      headers: { 'Content-Type': 'application/json' },
   465                      body: JSON.stringify({ system_prompt: promptText.value })
   466                  }
   467              );
   468  
   469              if (response.ok) {
   470                  alert("系统提示词已保存! WebSocket将重新连接以应用更改。");
```

===== 第35页 =====

```
   471                  // 重新连接WebSocket以应用新的系统提示词
   472                  if (window.reconnectWebSocket) {
   473                      window.reconnectWebSocket();
   474                  }
   475              }
   476          } catch (error) {
   477              console.error("保存失败:", error);
   478              alert("保存失败");
   479          }
   480      }
   481  
   482      openConfigPanel() {
   483          const panel = document.getElementById('config-panel');
   484          if (panel) {
   485              panel.style.display = 'flex';
   486          }
   487      }
   488  
   489      closeConfigPanel() {
   490          const panel = document.getElementById('config-panel');
   491          if (panel) {
   492              panel.style.display = 'none';
   493          }
   494      }
   495  
   496      // ==================== 拍照功能 ====================
   497  
   498      setupCameraListeners() {
   499          const cameraBtn = document.getElementById('camera-button');
   500          const closePhotoBtn = document.getElementById('close-photo');
   501          const downloadBtn = document.getElementById('download-photo');
   502  
   503          if (cameraBtn) {
   504              cameraBtn.addEventListener('click', () => this.takePhoto());
   505          }
   506          if (closePhotoBtn) {
   507              closePhotoBtn.addEventListener('click', () => this.closePhotoModal());
   508          }
   509          if (downloadBtn) {
   510              downloadBtn.addEventListener('click', () => this.downloadPhoto());
   511          }
   512      }
   513  
   514      takePhoto() {
   515          const scene = document.querySelector('a-scene');
   516          if (!scene) return;
   517  
   518          // 获取canvas并转换为图片
   519          const canvas = scene.components.screenshot.getCanvas('perspective');
   520          if (canvas) {
```

===== 第36页 =====

```
   521              this.currentPhoto = canvas.toDataURL('image/png');
   522              this.showPhotoPreview(this.currentPhoto);
   523          } else {
   524              // 备用方案：直接从renderer获取
   525              const renderer = scene.renderer;
   526              renderer.render(scene.object3D, scene.camera);
   527              this.currentPhoto = renderer.domElement.toDataURL('image/png');
   528              this.showPhotoPreview(this.currentPhoto);
   529          }
   530      }
   531  
   532      showPhotoPreview(dataUrl) {
   533          const modal = document.getElementById('photo-modal');
   534          const preview = document.getElementById('photo-preview');
   535  
   536          if (modal && preview) {
   537              preview.src = dataUrl;
   538              modal.style.display = 'flex';
   539          }
   540      }
   541  
   542      closePhotoModal() {
   543          const modal = document.getElementById('photo-modal');
   544          if (modal) {
   545              modal.style.display = 'none';
   546          }
   547      }
   548  
   549      downloadPhoto() {
   550          if (!this.currentPhoto) return;
   551  
   552          const link = document.createElement('a');
   553          const timestamp = new Date().toISOString().replace(/[:.]/g, '-').slice(0, 19);
   554          link.download = `心灵世界_${timestamp}.png`;
   555          link.href = this.currentPhoto;
   556          link.click();
   557      }
   558  
   559      // ==================== 分享功能 ====================
   560  
   561      setupShareListeners() {
   562          const shareBtn = document.getElementById('share-button');
   563          const closeShareBtn = document.getElementById('close-share');
   564          const generateBtn = document.getElementById('generate-share');
   565          const copyLinkBtn = document.getElementById('copy-link');
   566          const copyAllBtn = document.getElementById('copy-all');
   567  
   568          if (shareBtn) {
   569              shareBtn.addEventListener('click', () => this.openShareModal());
   570          }
```

===== 第37页 =====

```
   571          if (closeShareBtn) {
   572              closeShareBtn.addEventListener('click', () => this.closeShareModal());
   573          }
   574          if (generateBtn) {
   575              generateBtn.addEventListener('click', () => this.generateShareLink());
   576          }
   577          if (copyLinkBtn) {
   578              copyLinkBtn.addEventListener('click', () => this.copyShareLink());
   579          }
   580          if (copyAllBtn) {
   581              copyAllBtn.addEventListener('click', () => this.copyAll());
   582          }
   583      }
   584  
   585      async openShareModal() {
   586          const modal = document.getElementById('share-modal');
   587          if (!modal) return;
   588  
   589          modal.style.display = 'flex';
   590  
   591          // 检查是否已有分享链接
   592          if (this.currentWorldId) {
   593              await this.checkExistingShare();
   594          }
   595      }
   596  
   597      closeShareModal() {
   598          const modal = document.getElementById('share-modal');
   599          if (modal) {
   600              modal.style.display = 'none';
   601          }
   602      }
   603  
   604      async checkExistingShare() {
   605          try {
   606              const response = await fetch(`${this.apiBase}/api/share/world/${this.currentWorldId}`);
   607              if (response.ok) {
   608                  const data = await response.json();
   609                  if (data.share_code) {
   610                      this.showShareInfo(data.share_code, data.view_count || 0);
   611                  }
   612              }
   613          } catch (error) {
   614              console.log("未找到现有分享");
   615          }
   616      }
   617  
   618      async generateShareLink() {
   619          if (!this.currentWorldId) {
   620              this.showShareStatus('请先登录', 'error');
```

===== 第38页 =====

```
   621              return;
   622          }
   623  
   624          const generateBtn = document.getElementById('generate-share');
   625          if (generateBtn) generateBtn.disabled = true;
   626          this.showShareStatus('正在生成...', 'info');
   627  
   628          try {
   629              const response = await fetch(`${this.apiBase}/api/share/${this.currentWorldId}`, {
   630                  method: 'POST'
   631              });
   632  
   633              if (response.ok) {
   634                  const data = await response.json();
   635                  this.showShareInfo(data.share_code, 0);
   636                  this.showShareStatus('分享链接已生成!', 'success');
   637              } else {
   638                  this.showShareStatus('生成失败，请重试', 'error');
   639              }
   640          } catch (error) {
   641              this.showShareStatus('网络错误，请重试', 'error');
   642          } finally {
   643              if (generateBtn) generateBtn.disabled = false;
   644          }
   645      }
   646  
   647      showShareInfo(shareCode, viewCount) {
   648          const linkContainer = document.getElementById('share-link-container');
   649          const generateContainer = document.getElementById('share-generate');
   650          const linkInput = document.getElementById('share-link');
   651          const textArea = document.getElementById('share-text');
   652          const viewCountEl = document.getElementById('view-count');
   653  
   654          if (linkContainer) linkContainer.style.display = 'block';
   655          if (generateContainer) generateContainer.style.display = 'none';
   656  
   657          // 构建分享链接
   658          const baseUrl = window.location.origin + window.location.pathname;
   659          const shareUrl = `${baseUrl}?share=${shareCode}`;
   660  
   661          if (linkInput) {
   662              linkInput.value = shareUrl;
   663              this.currentShareUrl = shareUrl;
   664          }
   665  
   666          // 生成分享文案
   667          const username = localStorage.getItem('username') || '我';
   668          const shareText = `${username}邀请你来探索TA的心灵世界！\n\n这是一个充满魔法的森林，有可爱的精灵等你来聊天~\n\n点击链接进入：${shareUrl}`;
   669  
   670          if (textArea) {
```

===== 第39页 =====

```
   671              textArea.value = shareText;
   672              this.currentShareText = shareText;
   673          }
   674  
   675          if (viewCountEl) {
   676              viewCountEl.textContent = viewCount;
   677          }
   678      }
   679  
   680      showShareStatus(message, type) {
   681          const statusEl = document.getElementById('share-status');
   682          if (!statusEl) return;
   683  
   684          statusEl.textContent = message;
   685          statusEl.style.color = type === 'error' ? '#f87171' :
   686                                 type === 'success' ? '#4ade80' : '#7ec8e3';
   687      }
   688  
   689      async copyShareLink() {
   690          if (!this.currentShareUrl) return;
   691  
   692          try {
   693              await navigator.clipboard.writeText(this.currentShareUrl);
   694              this.showShareStatus('链接已复制!', 'success');
   695          } catch (error) {
   696              // 备用复制方法
   697              this.fallbackCopy(this.currentShareUrl);
   698              this.showShareStatus('链接已复制!', 'success');
   699          }
   700      }
   701  
   702      async copyAll() {
   703          if (!this.currentShareText) return;
   704  
   705          try {
   706              await navigator.clipboard.writeText(this.currentShareText);
   707              this.showShareStatus('链接和文案已复制!', 'success');
   708          } catch (error) {
   709              this.fallbackCopy(this.currentShareText);
   710              this.showShareStatus('链接和文案已复制!', 'success');
   711          }
   712      }
   713  
   714      fallbackCopy(text) {
   715          const textArea = document.createElement('textarea');
   716          textArea.value = text;
   717          textArea.style.position = 'fixed';
   718          textArea.style.left = '-9999px';
   719          document.body.appendChild(textArea);
   720          textArea.select();
```

===== 第40页 =====

```
   721          document.execCommand('copy');
   722          document.body.removeChild(textArea);
   723      }
   724  
   725      // ==================== 访客模式 ====================
   726  
   727      checkVisitorMode() {
   728          const urlParams = new URLSearchParams(window.location.search);
   729          const shareCode = urlParams.get('share');
   730  
   731          if (shareCode) {
   732              this.isVisitorMode = true;
   733              this.loadSharedWorld(shareCode);
   734              return true;
   735          }
   736          return false;
   737      }
   738  
   739      async loadSharedWorld(shareCode) {
   740          try {
   741              const response = await fetch(`${this.apiBase}/api/share/${shareCode}`);
   742              if (response.ok) {
   743                  const world = await response.json();
   744                  this.applyVisitorWorld(world);
   745              } else {
   746                  alert('分享链接无效或已过期');
   747                  this.showDefaultScene();
   748                  window.location.href = window.location.pathname;
   749              }
   750          } catch (error) {
   751              console.error('加载分享世界失败:', error);
   752              this.showDefaultScene();
   753          }
   754      }
   755  
   756      applyVisitorWorld(world) {
   757          // 设置当前世界ID以便WebSocket使用正确的system_prompt
   758          if (world.id) {
   759              this.currentWorldId = world.id;
   760              window.currentWorldId = world.id;
   761          }
   762  
   763          // 显示访客提示条
   764          const banner = document.getElementById('visitor-banner');
   765          const worldName = document.getElementById('visitor-world-name');
   766  
   767          if (banner) banner.style.display = 'block';
   768          if (worldName) worldName.textContent = `正在浏览: ${world.name || '分享的世界'}`;
   769  
   770          // 隐藏菜单中不允许的按钮（设置和分享）
```

===== 第41页 =====

```
   771          const configBtn = document.getElementById('config-button');
   772          const shareBtn = document.getElementById('share-button');
   773          if (configBtn) configBtn.remove();
   774          if (shareBtn) shareBtn.remove();
   775  
   776          // 隐藏登录弹窗
   777          const usernameModal = document.getElementById('username-modal');
   778          if (usernameModal) usernameModal.style.display = 'none';
   779  
   780          // 应用世界配置
   781          const scene = document.querySelector('a-scene');
   782          const applyConfig = () => {
   783              // 天空贴图
   784              if (world.sky_texture) {
   785                  this.updateSkyTexture(`${this.apiBase}/uploads/${world.sky_texture}`);
   786              } else {
   787                  const sky = document.querySelector('#sky');
   788                  if (sky) sky.setAttribute('visible', 'true');
   789              }
   790  
   791              // 身外化身模型
   792              if (world.fairy_model) {
   793                  this.updateFairyModel(`${this.apiBase}/uploads/${world.fairy_model}`);
   794              } else {
   795                  // 没有自定义，加载默认模型
   796                  const fairy = document.querySelector('#fairy');
   797                  if (fairy) {
   798                      fairy.setAttribute('gltf-model', '#fairyModel');
   799                      fairy.addEventListener('model-loaded', () => {
   800                          fairy.setAttribute('visible', 'true');
   801                      }, { once: true });
   802                  }
   803              }
   804  
   805              // 背景音乐
   806              if (world.background_music) {
   807                  const music = document.querySelector('#background-music');
   808                  if (music) music.src = `${this.apiBase}/uploads/${world.background_music}`;
   809              }
   810  
   811              // 开场白
   812              if (world.greeting_message) {
   813                  window.customGreeting = world.greeting_message;
   814              }
   815          };
   816  
   817          if (scene && scene.hasLoaded) {
   818              applyConfig();
   819          } else if (scene) {
   820              scene.addEventListener('loaded', applyConfig, { once: true });
```

===== 第42页 =====

```
   821          }
   822  
   823          // 绑定创建自己世界按钮
   824          const createBtn = document.getElementById('create-own-world');
   825          if (createBtn) {
   826              createBtn.addEventListener('click', () => {
   827                  window.location.href = window.location.pathname;
   828              });
   829          }
   830      }
   831  }
   832  
   833  // 创建全局实例
   834  const worldConfig = new WorldConfig();
   835  
   836  // 页面加载后初始化
   837  document.addEventListener('DOMContentLoaded', () => {
   838      // 先检查访客模式
   839      const isVisitorMode = worldConfig.checkVisitorMode();
   840  
   841      // 如果不是访客模式，初始化配置
   842      if (!isVisitorMode) {
   843          worldConfig.init();
   844      }
   845  
   846      // 设置拍照和分享监听器
   847      worldConfig.setupCameraListeners();
   848      worldConfig.setupShareListeners();
   849  });
```

### 文件: chat.js

**路径:** ../frontend/chat.js  
**说明:** 聊天模块，WebSocket通信  
**行数:** 237行

```
     1  let socket;
     2  let conversation = []; // 存储当前对话的上下文
     3  let currentLlmId = null; // 当前选择的LLM ID（null表示使用默认）
     4  let heartbeatInterval = null; // 心跳定时器
     5  let reconnectAttempts = 0; // 重连次数
     6  const MAX_RECONNECT_ATTEMPTS = 5; // 最大重连次数
     7  let isManualClose = false; // 是否手动关闭
     8  
     9  function connectWebSocket() {
    10      // 如果已经连接，不重复连接
    11      if (socket && socket.readyState === WebSocket.OPEN) {
    12          console.log("WebSocket已连接，无需重连");
    13          return;
    14      }
    15  
    16      // 获取当前世界ID以应用正确的system_prompt
    17      const worldId = window.currentWorldId;
    18      const worldIdParam = worldId ? `?world_id=${worldId}` : '';
    19  
    20      if (window.location.hostname === "127.0.0.1" || window.location.hostname === "localhost") {
    21          socket = new WebSocket(`ws://127.0.0.1:8000/ws/chat_with_claude/${worldIdParam}`);
```

===== 第43页 =====

```
    22      } else {
    23          socket = new WebSocket(`wss://api.psyventures.cn/ws/chat_with_claude/${worldIdParam}`);
    24      }
    25  
    26      socket.onopen = function () {
    27          console.log("WebSocket连接已建立");
    28          reconnectAttempts = 0; // 重置重连次数
    29          isManualClose = false;
    30          startHeartbeat();
    31      };
    32  
    33      socket.onmessage = function (event) {
    34          // 忽略心跳响应
    35          if (event.data === "pong") return;
    36  
    37          if (event.data === "[END_OF_RESPONSE]") {
    38              let lastDialogue = document.getElementsByClassName('companion-dialogue-active')[0];
    39              if (lastDialogue) {
    40                  lastDialogue.classList.remove('companion-dialogue-active');
    41                  lastDialogue.classList.add('companion-dialogue-finished');
    42              }
    43          } else {
    44              let lastDialogue = document.getElementsByClassName('companion-dialogue-active')[0];
    45              const dialogueContent = document.getElementById('dialogue-content');
    46  
    47              if (!lastDialogue) {
    48                  lastDialogue = document.createElement('div');
    49                  lastDialogue.classList.add('dialogue-entry', 'companion-dialogue', 'companion-dialogue-active');
    50                  dialogueContent.appendChild(lastDialogue);
    51              }
    52  
    53              lastDialogue.innerHTML += event.data;
    54              dialogueContent.scrollTop = dialogueContent.scrollHeight;
    55          }
    56      };
    57  
    58      socket.onclose = function (event) {
    59          console.log("WebSocket连接已关闭", event.code, event.reason);
    60          stopHeartbeat();
    61  
    62          // 如果不是手动关闭，尝试自动重连
    63          if (!isManualClose && reconnectAttempts < MAX_RECONNECT_ATTEMPTS) {
    64              reconnectAttempts++;
    65              const delay = Math.min(1000 * reconnectAttempts, 5000); // 递增延迟，最多5秒
    66              console.log(`将在 ${delay}ms 后尝试第 ${reconnectAttempts} 次重连...`);
    67              setTimeout(connectWebSocket, delay);
    68          }
    69      };
    70  
    71      socket.onerror = function (error) {
```

===== 第44页 =====

```
    72          console.error("WebSocket发生错误:", error);
    73      };
    74  }
    75  
    76  // 心跳机制，保持连接活跃
    77  function startHeartbeat() {
    78      stopHeartbeat();
    79      heartbeatInterval = setInterval(() => {
    80          if (socket && socket.readyState === WebSocket.OPEN) {
    81              socket.send(JSON.stringify({ type: "ping" }));
    82          }
    83      }, 30000); // 每30秒发送一次心跳
    84  }
    85  
    86  function stopHeartbeat() {
    87      if (heartbeatInterval) {
    88          clearInterval(heartbeatInterval);
    89          heartbeatInterval = null;
    90      }
    91  }
    92  
    93  // 展示开场白
    94  function renderOpeningDialogue() {
    95      // 优先使用自定义的greeting，如果没有则使用默认文本
    96      const defaultGreeting = "欢迎来到这片充满魔法的森林！(^_^) 我是森林精灵絮语，很高兴遇见你~ 这里是一个可以分享任何心事的安全港湾，让我们一起探索这片充满治愈力量的地方吧！";
    97      const openingText = window.customGreeting || defaultGreeting;
    98  
    99      const dialogueContent = document.getElementById("dialogue-content");
   100      const openingDialogue = document.createElement('div');
   101      openingDialogue.id = 'opening-dialogue';
   102      openingDialogue.classList.add('dialogue-entry', 'companion-dialogue', 'companion-dialogue-finished');
   103      openingDialogue.innerHTML = openingText;
   104      dialogueContent.appendChild(openingDialogue);
   105      dialogueContent.scrollTop = dialogueContent.scrollHeight;
   106  }
   107  
   108  // 更新开场白（用于设置保存后）- 暴露到全局以便world-config.js调用
   109  window.updateOpeningDialogue = function(newGreeting) {
   110      const openingDialogue = document.getElementById('opening-dialogue');
   111      if (openingDialogue && newGreeting) {
   112          openingDialogue.innerHTML = newGreeting;
   113          console.log("开场白已更新");
   114      }
   115  };
   116  
   117  // 重新连接WebSocket（用于system_prompt更新后）- 暴露到全局以便world-config.js调用
   118  window.reconnectWebSocket = function() {
   119      if (socket) {
   120          isManualClose = true; // 标记为手动关闭，避免触发自动重连
   121          socket.close();
```

===== 第45页 =====

```
   122      }
   123      // 短暂延迟后重新连接
   124      setTimeout(() => {
   125          reconnectAttempts = 0; // 重置重连次数
   126          connectWebSocket();
   127          console.log("WebSocket已重新连接以应用新的系统提示词");
   128      }, 500);
   129  };
   130  
   131  // 渲染用户对话
   132  function renderUserDialogue(text) {
   133      const dialogueContent = document.getElementById('dialogue-content');
   134      const userDialogue = document.createElement('div');
   135      userDialogue.classList.add('dialogue-entry', 'user-dialogue');
   136      userDialogue.innerHTML = text;
   137      dialogueContent.appendChild(userDialogue);
   138      dialogueContent.scrollTop = dialogueContent.scrollHeight;
   139  }
   140  
   141  // 发送用户消息给WebSocket服务器
   142  function sendUserMessage() {
   143      const inputBox = document.getElementById('input-box');
   144      const userText = inputBox.value.trim();
   145      if (userText !== "") {
   146          renderUserDialogue(userText);
   147  
   148          conversation.push({ role: "user", content: userText });
   149          const requestData = {
   150              content: userText,
   151              llm_id: currentLlmId  // 使用选择的LLM
   152          };
   153  
   154          if (socket.readyState === WebSocket.OPEN) {
   155              socket.send(JSON.stringify(requestData));
   156          } else {
   157              console.error("WebSocket未连接，无法发送消息");
   158          }
   159  
   160          inputBox.value = "";
   161          document.getElementById('dialogue-content').scrollTop = document.getElementById('dialogue-content').scrollHeight;
   162      }
   163  }
   164  
   165  document.getElementById('send-button').addEventListener('click', () => {
   166      sendUserMessage();
   167  });
   168  
   169  document.getElementById('input-box').addEventListener('keypress', (event) => {
   170      if (event.key === 'Enter') {
   171          sendUserMessage();
```

===== 第46页 =====

```
   172      }
   173  });
   174  
   175  document.addEventListener("DOMContentLoaded", () => {
   176      // 延迟渲染开场白，等待world-config.js设置customGreeting
   177      setTimeout(() => {
   178          renderOpeningDialogue();
   179      }, 800);
   180  
   181      const usernameModal = document.getElementById("username-modal");
   182      const usernameInput = document.getElementById("username-input");
   183      const submitButton = document.getElementById("submit-username");
   184      const cancelUsernameBtn = document.getElementById('cancel-username');
   185  
   186      // 检查用户是否已登录，如果没有则显示登录弹窗
   187      const savedUsername = localStorage.getItem("username");
   188      if (!savedUsername) {
   189          usernameModal.style.display = 'flex';
   190      }
   191  
   192      cancelUsernameBtn.addEventListener('click', function () {
   193          // 隐藏弹窗
   194          usernameModal.style.display = 'none';
   195      });
   196      submitButton.addEventListener("click", async () => {
   197          const username = usernameInput.value.trim();
   198          if (username) {
   199              try {
   200                  let url;
   201                  if (window.location.hostname === "127.0.0.1" || window.location.hostname === "localhost")
   202                      url = "http://127.0.0.1:8000/set_username/";
   203                  else
   204                      url = "https://api.psyventures.cn/set_username/";
   205                  const response = await fetch(url, {
   206                      method: "POST",
   207                      headers: {
   208                          "Content-Type": "application/json"
   209                      },
   210                      body: JSON.stringify({ username })
   211                  });
   212  
   213                  if (!response.ok) {
   214                      throw new Error(`请求失败，状态码: ${response.status}`);
   215                  }
   216  
   217                  const result = await response.json();
   218                  console.log("API 执行结果:", result);
   219  
   220                  usernameModal.style.display = "none";
   221                  localStorage.setItem("username", username);
```

===== 第47页 =====

```
   222              } catch (error) {
   223                  console.error("用户名设置失败:", error);
   224                  alert("无法设置用户名，请重试。");
   225              }
   226          } else {
   227              alert("请输入用户名");
   228          }
   229      });
   230  });
   231  
   232  // 延迟连接WebSocket，等待世界配置加载完成
   233  // 如果world-config.js在1秒内设置了currentWorldId，则使用它连接
   234  // 否则使用默认配置连接
   235  setTimeout(() => {
   236      connectWebSocket();
   237  }, 1000);
```

### 文件: component.js

**路径:** ../frontend/component.js  
**说明:** UI组件和交互控制  
**行数:** 180行

```
     1  
     2  // 菜单展开/收起控制
     3  const menuToggle = document.getElementById('menu-toggle');
     4  const menuItems = document.getElementById('menu-items');
     5  
     6  if (menuToggle && menuItems) {
     7      menuToggle.addEventListener('click', function() {
     8          menuToggle.classList.toggle('active');
     9          menuItems.classList.toggle('show');
    10      });
    11  
    12      // 点击菜单项后自动收起菜单（除了需要保持开启的按钮）
    13      document.querySelectorAll('.menu-item').forEach(item => {
    14          item.addEventListener('click', function() {
    15              // 音乐和自动播放按钮点击后不收起菜单
    16              if (this.id !== 'music-button' && this.id !== 'autoplay-button') {
    17                  menuToggle.classList.remove('active');
    18                  menuItems.classList.remove('show');
    19              }
    20          });
    21      });
    22  
    23      // 点击其他区域收起菜单
    24      document.addEventListener('click', function(e) {
    25          if (!e.target.closest('#menu-container')) {
    26              menuToggle.classList.remove('active');
    27              menuItems.classList.remove('show');
    28          }
    29      });
    30  }
    31  
    32  // 更新的全屏控制代码
    33  const fullscreenButton = document.getElementById('fullscreen-button');
    34  fullscreenButton.addEventListener('click', function () {
```

===== 第48页 =====

```
    35      if (!document.fullscreenElement && !document.mozFullScreenElement &&
    36          !document.webkitFullscreenElement && !document.msFullscreenElement) {
    37          // 进入全屏
    38          if (document.documentElement.requestFullscreen) {
    39              document.documentElement.requestFullscreen();
    40          } else if (document.documentElement.msRequestFullscreen) {
    41              document.documentElement.msRequestFullscreen();
    42          } else if (document.documentElement.mozRequestFullScreen) {
    43              document.documentElement.mozRequestFullScreen();
    44          } else if (document.documentElement.webkitRequestFullscreen) {
    45              document.documentElement.webkitRequestFullscreen(Element.ALLOW_KEYBOARD_INPUT);
    46          }
    47      } else {
    48          // 退出全屏
    49          if (document.exitFullscreen) {
    50              document.exitFullscreen();
    51          } else if (document.msExitFullscreen) {
    52              document.msExitFullscreen();
    53          } else if (document.mozCancelFullScreen) {
    54              document.mozCancelFullScreen();
    55          } else if (document.webkitExitFullscreen) {
    56              document.webkitExitFullscreen();
    57          }
    58      }
    59  });
    60  
    61  // F11 键控制全屏
    62  document.addEventListener('keydown', function (event) {
    63      if (event.key === 'F11') {
    64          event.preventDefault();
    65          fullscreenButton.click();
    66      }
    67  });
    68  
    69  // 渲染对话条目
    70  let currentDialogueIndex = 0;
    71  const dialogueContent = document.getElementById('dialogue-content');
    72  const expandButton = document.getElementById('expand-button');
    73  let isExpanded = false;
    74  function renderDialogue(entry) {
    75      const dialogueEntry = document.createElement('div');
    76      dialogueEntry.classList.add('dialogue-entry');
    77      if (entry.speaker === "User") {
    78          dialogueEntry.classList.add('user-dialogue');
    79          dialogueEntry.innerHTML = `<span>${entry.text}</span>`;
    80      } else {
    81          dialogueEntry.classList.add('companion-dialogue');
    82          dialogueEntry.innerHTML = `<span>${entry.text}</span>`;
    83      }
    84      dialogueContent.appendChild(dialogueEntry);
```

===== 第49页 =====

```
    85  }
    86  
    87  // 添加音乐按钮点击事件
    88  const musicButton = document.getElementById('music-button');
    89  const backgroundMusic = document.getElementById('background-music');
    90  let isPlaying = false;
    91  
    92  // 确保音频元素正确加载
    93  backgroundMusic.load();
    94  
    95  musicButton.addEventListener('click', function () {
    96      if (!isPlaying) {
    97          // 尝试播放音乐
    98          const playPromise = backgroundMusic.play();
    99          if (playPromise !== undefined) {
   100              playPromise.then(() => {
   101                  isPlaying = true;
   102                  musicButton.style.background = 'rgba(0, 0, 0, 0.5)';
   103                  musicButton.querySelector('svg path').style.fill = 'white';
   104              }).catch((error) => {
   105                  console.error('音乐播放失败:', error);
   106                  // 如果是自动播放策略阻止，提示用户
   107                  if (error.name === 'NotAllowedError') {
   108                      alert('请点击页面任意位置后再试');
   109                  }
   110              });
   111          }
   112      } else {
   113          backgroundMusic.pause();
   114          isPlaying = false;
   115          musicButton.style.background = 'rgba(0, 0, 0, 0.3)';
   116          musicButton.querySelector('svg path').style.fill = 'rgba(255, 255, 255, 0.3)';
   117      }
   118  });
   119  
   120  // 注册 tour 组件，用于自动播放 sky
   121  AFRAME.registerComponent('tour', {
   122      schema: {
   123          speed: { type: 'number', default: 5 } // 默认旋转速度
   124      },
   125      init: function () {
   126          this.yaw = this.el.getAttribute('rotation').y || 0; // 初始化旋转角度
   127      },
   128      tick: function (time, timeDelta) {
   129          // 每一帧中更新旋转角度，使得 sky 进行旋转
   130          this.yaw += this.data.speed * timeDelta / 1000;
   131          this.el.setAttribute('rotation', { x: 0, y: this.yaw, z: 0 });
   132      }
   133  });
   134  
```

===== 第50页 =====

```
   135  // 获取 autoplay 按钮和 <a-sky> 元素
   136  const autoplayButton = document.getElementById('autoplay-button');
   137  const sky = document.querySelector('#sky'); // 获取 a-sky 元素
   138  
   139  let isAutoplayEnabled = false;
   140  // 点击按钮切换自动播放状态
   141  autoplayButton.addEventListener('click', function () {
   142      isAutoplayEnabled = !isAutoplayEnabled;
   143  
   144      if (isAutoplayEnabled) {
   145          // 添加自动播放组件到 a-sky 元素
   146          sky.setAttribute('tour', 'speed: 5');
   147          // 更新按钮图标，变为暂停
   148          autoplayButton.querySelector('svg path').setAttribute('d', 'M384 192h256v640H384z'); // 改为暂停图标
   149      } else {
   150          // 移除自动播放组件
   151          sky.removeAttribute('tour');
   152          // 更新按钮图标，变为播放
   153          autoplayButton.querySelector('svg path').setAttribute('d', 'M896 512L384 832V192z'); // 改为播放图标
   154      }
   155  });
   156  
   157  // 展开按钮功能
   158  expandButton.addEventListener('click', () => {
   159      if (isExpanded) {
   160          dialogueContent.style.height = '15vh';
   161          expandButton.innerHTML = `<svg xmlns="http://www.w3.org/2000/svg" width="2em" height="2em" viewBox="0 0 24 24"> <path fill="rgba(255, 255, 255, 0.5)" d="m12 11.325l2.375 2.375q.275.275.688.275t.712-.275q.3-.3.3-.712t-.3-.713L12.7 9.2q-.3-.3-.7-.3t-.7.3l-3.1 3.1q-.3.3-.287.7t.312.7q.3.275.7.288t.7-.288zM12 22q-2.075 0-3.9-.788t-3.175-2.137T2.788 15.9T2 12t.788-3.9t2.137-3.175T8.1 2.788T12 2t3.9.788t3.175 2.137T21.213 8.1T22 12t-.788 3.9t-2.137 3.175t-3.175 2.138T12 22"/></svg>`;
   162      } else {
   163          dialogueContent.style.height = '80vh';
   164          expandButton.innerHTML = `<svg xmlns="http://www.w3.org/2000/svg" width="2em" height="2em" viewBox="0 0 24 24"> <path fill="rgba(255, 255, 255, 0.5)" d="M12 12.675L9.625 10.3q-.275-.275-.687-.275t-.713.275q-.3.3-.3.713t.3.712L11.3 14.8q.3.3.7.3t.7-.3l3.1-3.1q.3-.3.287-.7t-.312-.7q-.3-.275-.7-.288t-.7.288zM12 22q-2.075 0-3.9-.788t-3.175-2.137T2.788 15.9T2 12t.788-3.9t2.137-3.175T8.1 2.788T12 2t3.9.788t3.175 2.137T21.213 8.1T22 12t-.788 3.9t-2.137 3.175t-3.175 2.138T12 22"/></svg>`;
   165      }
   166      isExpanded = !isExpanded;
   167  });
   168  
   169  // 鼠标滚轮控制前后移动
   170  document.querySelector('a-scene').addEventListener('wheel', function (event) {
   171      const rig = document.querySelector('#rig'); // 获取摄像机容器元素（rig）
   172      const moveStep = 1; // 每次滚动移动的距离
   173      if (event.deltaY < 0) {
   174          // 向上滚动鼠标滚轮，向前移动视角
   175          rig.object3D.position.z -= moveStep;
   176      } else {
   177          // 向下滚动鼠标滚轮，向后移动视角
   178          rig.object3D.position.z += moveStep;
   179      }
   180  });
```

### 文件: index.html

**路径:** ../frontend/index.html  
**说明:** HTML主页面  
**行数:** 288行

```
     1  <!DOCTYPE html>
     2  <html>
     3  
     4  <head>
```

===== 第51页 =====

```
     5    <meta charset="utf-8">
     6    <meta name="viewport" content="width=device-width, initial-scale=1.0">
     7    <title>A-Frame WebVR Example with Fixed Fairy and Angry Stone Interaction</title>
     8    <script src="./aframe.min.js"></script>
     9    <link rel="stylesheet" type="text/css" href="style.css">
    10  
    11  </head>
    12  <body>
    13    <!-- 用户名弹窗 -->
    14    <div id="username-modal"
    15    style="display: none; flex-direction: column; align-items: center; justify-content: center; position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; background: rgba(0, 0, 0, 0.7); z-index: 9999999;">
    16   <div style="background: white; padding: 20px; border-radius: 8px; text-align: center;">
    17       <h2>请输入您的用户名</h2>
    18       <input type="text" id="username-input" placeholder="输入用户名" 
    19              style="padding: 8px; width: 100%; margin-top: 10px;" />
    20       <div style="display: flex; justify-content: space-between; margin-top: 10px;">
    21           <button id="submit-username" style="padding: 8px 16px; flex-grow: 1; margin-right: 10px;">确认</button>
    22           <button id="cancel-username" style="padding: 8px 16px; flex-grow: 1; background-color: #f0f0f0;">取消</button>
    23       </div>
    24   </div>
    25  </div>
    26    <!-- 对话框区域 -->
    27    <div id="dialogue-container">
    28      <!-- 展开按钮 -->
    29      <button id="expand-button">
    30        <!-- 插入展开按钮的 SVG 图标 -->
    31        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24">
    32          <path fill="rgba(255, 255, 255, 0.5)"
    33            d="m12 11.325l2.375 2.375q.275.275.688.275t.712-.275q.3-.3.3-.712t-.3-.713L12.7 9.2q-.3-.3-.7-.3t-.7.3l-3.1 3.1q-.3.3-.287.7t.312.7q.3.275.7.288t.7-.288zM12 22q-2.075 0-3.9-.788t-3.175-2.137T2.788 15.9T2 12t.788-3.9t2.137-3.175T8.1 2.788T12 2t3.9.788t3.175 2.137T21.213 8.1T22 12t-.788 3.9t-2.137 3.175t-3.175 2.138T12 22" />
    34        </svg>
    35      </button>
    36      <!-- 对话内容 -->
    37      <div id="dialogue-content" style="height: 15vh;"></div>
    38  
    39      <!-- 对话输入区域 -->
    40      <div id="dialogue-input">
    41        <input type="text" id="input-box" placeholder="一起聊聊天...">
    42        <button id="send-button">
    43          <!-- 插入SVG图标 -->
    44          <svg id="icon-svg" viewBox="0 0 20 20" width="24" height="24" xmlns="http://www.w3.org/2000/svg">
    45          <g fill="currentColor" fill-rule="evenodd" clip-rule="evenodd"><path d="M2.5 8a.5.5 0 0 1 .5.5V17h14V8.5a.5.5 0 0 1 1 0v9a.5.5 0 0 1-.5.5h-15a.5.5 0 0 1-.5-.5v-9a.5.5 0 0 1 .5-.5"/><path d="M3 5.5a.5.5 0 0 1 .5-.5h13a.5.5 0 0 1 .5.5v4.67a.5.5 0 0 1-.223.416l-6.5 4.33a.5.5 0 0 1-.554 0l-6.5-4.33A.5.5 0 0 1 3 10.17zM4 6v3.902l6 3.997l6-3.997V6z"/><path d="M9.723 2.084a.5.5 0 0 1 .554 0l4.5 3l-.554.832L10 3.101L5.777 5.916l-.554-.832zm7.131 5.062l1 1l-.708.708l-1-1zm-13 .708l-1 1l-.708-.708l1-1zM6.75 8A.25.25 0 0 1 7 7.75h6a.25.25 0 1 1 0 .5H7A.25.25 0 0 1 6.75 8m.5 2a.25.25 0 0 1 .25-.25h5a.25.25 0 1 1 0 .5h-5a.25.25 0 0 1-.25-.25"/></g>
    46          </svg>
    47        </button>
    48        <button id="voice-button">
    49          <!-- 插入语音按钮的 SVG 图标 -->
    50          <svg id="icon-svg" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 20 20">
    51            <path fill="currentColor" 
    52            d="M17.132 1.06a.5.5 0 0 1 .677.203A9.96 9.96 0 0 1 19 6a9.96 9.96 0 0 1-1.19 4.737a.5.5 0 1 1-.881-.474A8.96 8.96 0 0 0 18 6a8.96 8.96 0 0 0-1.071-4.263a.5.5 0 0 1 .203-.677m-2.325 1.974a.5.5 0 0 1 .654.269A7 7 0 0 1 16 6a7 7 0 0 1-.539 2.697a.5.5 0 1 1-.922-.385A6 6 0 0 0 15 6c0-.82-.164-1.6-.461-2.312a.5.5 0 0 1 .268-.654M5 6a4 4 0 1 1 8 0a4 4 0 0 1-8 0m4-3a3 3 0 1 0 0 6a3 3 0 0 0 0-6M2 13c0-1.113.903-2 2.009-2H14a2 2 0 0 1 2 2c0 1.691-.833 2.966-2.135 3.797C12.583 17.614 10.855 18 9 18s-3.583-.386-4.865-1.203C2.833 15.967 2 14.69 2 13m2.009-1C3.448 12 3 12.447 3 13c0 1.309.622 2.284 1.673 2.953C5.743 16.636 7.265 17 9 17s3.257-.364 4.327-1.047C14.377 15.283 15 14.31 15 13a1 1 0 0 0-1-1z"/>
    53          </svg>
    54        </button>
```

===== 第52页 =====

```
    55      </div>
    56    </div>
    57  
    58    <!-- 展开式菜单 -->
    59    <div id="menu-container">
    60      <!-- 主菜单按钮 -->
    61      <button id="menu-toggle" title="菜单">
    62        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24">
    63          <path fill="currentColor" d="M4 6h16v2H4zm0 5h16v2H4zm0 5h16v2H4z"/>
    64        </svg>
    65      </button>
    66  
    67      <!-- 展开的菜单项 -->
    68      <div id="menu-items">
    69        <button id="camera-button" class="menu-item" data-label="拍照">
    70          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24">
    71            <path fill="currentColor" d="M12 17.5a5.5 5.5 0 1 0 0-11a5.5 5.5 0 0 0 0 11m0-2a3.5 3.5 0 1 1 0-7a3.5 3.5 0 0 1 0 7M4 5h3l1.5-2h7L17 5h3a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V7a2 2 0 0 1 2-2"/>
    72          </svg>
    73          <span class="menu-label">拍照</span>
    74        </button>
    75  
    76        <button id="share-button" class="menu-item" data-label="分享">
    77          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24">
    78            <path fill="currentColor" d="M18 16.08c-.76 0-1.44.3-1.96.77L8.91 12.7c.05-.23.09-.46.09-.7s-.04-.47-.09-.7l7.05-4.11c.54.5 1.25.81 2.04.81a3 3 0 1 0-3-3c0 .24.04.47.09.7L8.04 9.81C7.5 9.31 6.79 9 6 9a3 3 0 1 0 0 6c.79 0 1.5-.31 2.04-.81l7.12 4.15c-.05.21-.08.43-.08.66a2.92 2.92 0 0 0 2.92 2.92A2.92 2.92 0 0 0 21 19a2.92 2.92 0 0 0-3-2.92"/>
    79          </svg>
    80          <span class="menu-label">分享</span>
    81        </button>
    82  
    83        <button id="config-button" class="menu-item" data-label="设置">
    84          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24">
    85            <path fill="currentColor" d="M19.14 12.94c.04-.31.06-.63.06-.94c0-.31-.02-.63-.06-.94l2.03-1.58a.49.49 0 0 0 .12-.61l-1.92-3.32a.49.49 0 0 0-.59-.22l-2.39.96c-.5-.38-1.03-.7-1.62-.94l-.36-2.54a.484.484 0 0 0-.48-.41h-3.84c-.24 0-.43.17-.47.41l-.36 2.54c-.59.24-1.13.57-1.62.94l-2.39-.96a.49.49 0 0 0-.59.22L2.74 8.87c-.12.21-.08.47.12.61l2.03 1.58c-.04.31-.06.63-.06.94s.02.63.06.94l-2.03 1.58a.49.49 0 0 0-.12.61l1.92 3.32c.12.22.37.29.59.22l2.39-.96c.5.38 1.03.7 1.62.94l.36 2.54c.05.24.24.41.48.41h3.84c.24 0 .44-.17.47-.41l.36-2.54c.59-.24 1.13-.56 1.62-.94l2.39.96c.22.08.47 0 .59-.22l1.92-3.32c.12-.22.07-.47-.12-.61zM12 15.6c-1.98 0-3.6-1.62-3.6-3.6s1.62-3.6 3.6-3.6s3.6 1.62 3.6 3.6s-1.62 3.6-3.6 3.6"/>
    86          </svg>
    87          <span class="menu-label">设置</span>
    88        </button>
    89  
    90        <button id="music-button" class="menu-item" data-label="音乐">
    91          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 512 512">
    92            <path fill="currentColor" d="M98.05 18.54c-11.46-.08-23.59 1.28-36.08 3.99L130.1 261.1c-14.2-5.1-31.18-6.2-48.09-3.2c-39.17 6.9-67.15 33.8-62.52 59.8c4.64 26.1 40.14 41.7 79.33 34.7c39.08-6.9 67.08-33.7 62.38-59.8c-22.1-80.8-44.4-163-66.39-244.21c50.69 3.59 72.59 31.58 90.59 60.31c-.5-62.33-37.7-89.81-87.35-90.16m310.65 30.7c-13.9.1-28.8 2.59-44.1 7.22l87.1 232.44c-14.6-3.9-31.6-3.7-48.3.7c-38.4 10-64.2 39-57.5 64.6c6.7 25.7 43.4 38.3 81.8 28.2c38.5-10 64.2-39 57.5-64.6c-28.6-78.8-57.3-158.9-85.8-238.2c50.8-.5 74.9 25.7 95.2 52.9c-5.2-58.55-40.4-83.61-85.9-83.26M258.4 163.5l1.8 248.1c-12.3-8.6-28.4-14.3-45.6-15.9c-39.4-3.8-73.7 14.5-76.2 41c-2.5 26.3 27.7 50.8 67.1 54.5c39.7 3.9 73.7-14.5 76.3-40.9c.2-83.8.8-168.9 1.3-253.1c47.9 17 61.6 49.8 71.2 82.4c19.9-74.1-27.9-112.3-95.9-116.1"/>
    93          </svg>
    94          <span class="menu-label">音乐</span>
    95        </button>
    96  
    97        <button id="autoplay-button" class="menu-item" data-label="自动播放">
    98          <svg viewBox="0 0 1024 1024" version="1.1" xmlns="http://www.w3.org/2000/svg">
    99            <path d="M896 512L384 832V192z"></path>
   100          </svg>
   101          <span class="menu-label">自动播放</span>
   102        </button>
   103  
   104        <button id="fullscreen-button" class="menu-item" data-label="全屏">
```

===== 第53页 =====

```
   105          <svg viewBox="0 0 1024 1024" version="1.1" xmlns="http://www.w3.org/2000/svg">
   106            <path d="M795.5 192H581c-19.6 0-35.6 15.7-36 35.3-0.4 20.3 16.4 36.7 36.7 36.7h128.4L581 393.1c-14 14-14 36.9 0 50.9s36.9 14 50.9 0L760 315.9v129c0 19.6 15.8 35.6 35.3 36 20.2 0.4 36.7-16.4 36.7-36.7V228.5c0-20.1-16.3-36.5-36.5-36.5zM442.2 760H313.8L443 630.9c14-14 14-36.9 0-50.9s-36.9-14-50.9 0L264 708.1V579c0-19.6-15.8-35.6-35.3-36-20.2-0.4-36.7 16.4-36.7 36.7v215.6c0 20.3 16.4 36.7 36.7 36.7H443c19.6 0 35.6-15.7 36-35.3 0.3-20.3-16.5-36.7-36.8-36.7z"/>
   107          </svg>
   108          <span class="menu-label">全屏</span>
   109        </button>
   110      </div>
   111    </div>
   112  
   113    <audio id="background-music" src="./forest_lake.mp3"></audio>
   114  
   115    <!-- 世界配置面板 -->
   116    <div id="config-panel" style="display: none; position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; background: rgba(0,0,0,0.8); z-index: 9999999; justify-content: center; align-items: center;">
   117      <div style="background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%); padding: 30px; border-radius: 16px; max-width: 500px; width: 90%; max-height: 80vh; overflow-y: auto; color: #fff; box-shadow: 0 20px 60px rgba(0,0,0,0.5);">
   118        <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px;">
   119          <h2 style="margin: 0; color: #d4af37;">自定义我的世界</h2>
   120          <button id="close-config" style="background: none; border: none; color: #fff; font-size: 24px; cursor: pointer;">&times;</button>
   121        </div>
   122  
   123        <!-- 天空贴图上传 -->
   124        <div class="config-section" style="margin-bottom: 20px;">
   125          <label style="display: block; margin-bottom: 8px; color: #a0a0a0;">天空背景图片</label>
   126          <div style="display: flex; gap: 10px; align-items: center;">
   127            <input type="file" id="sky-texture-input" accept=".jpg,.jpeg,.png,.webp,.hdr" style="display: none;">
   128            <button onclick="document.getElementById('sky-texture-input').click()" style="padding: 10px 20px; background: #4a4a6a; border: none; border-radius: 8px; color: #fff; cursor: pointer;">选择图片</button>
   129            <span id="sky-texture-status" style="color: #7ec8e3;"></span>
   130          </div>
   131          <p style="font-size: 12px; color: #666; margin-top: 5px;">支持格式: JPG, PNG, WebP, HDR</p>
   132        </div>
   133  
   134        <!-- 身外化身模型上传 -->
   135        <div class="config-section" style="margin-bottom: 20px;">
   136          <label style="display: block; margin-bottom: 8px; color: #a0a0a0;">身外化身3D模型</label>
   137          <div style="display: flex; gap: 10px; align-items: center;">
   138            <input type="file" id="fairy-model-input" accept=".gltf,.glb" style="display: none;">
   139            <button onclick="document.getElementById('fairy-model-input').click()" style="padding: 10px 20px; background: #4a4a6a; border: none; border-radius: 8px; color: #fff; cursor: pointer;">选择模型</button>
   140            <span id="fairy-model-status" style="color: #7ec8e3;"></span>
   141          </div>
   142          <p style="font-size: 12px; color: #666; margin-top: 5px;">支持格式: GLTF, GLB</p>
   143        </div>
   144  
   145        <!-- 背景音乐上传 -->
   146        <div class="config-section" style="margin-bottom: 20px;">
   147          <label style="display: block; margin-bottom: 8px; color: #a0a0a0;">背景音乐</label>
   148          <div style="display: flex; gap: 10px; align-items: center;">
   149            <input type="file" id="music-input" accept=".mp3,.wav,.ogg" style="display: none;">
   150            <button onclick="document.getElementById('music-input').click()" style="padding: 10px 20px; background: #4a4a6a; border: none; border-radius: 8px; color: #fff; cursor: pointer;">选择音乐</button>
   151            <span id="background-music-status" style="color: #7ec8e3;"></span>
   152          </div>
   153          <p style="font-size: 12px; color: #666; margin-top: 5px;">支持格式: MP3, WAV, OGG</p>
   154        </div>
```

===== 第54页 =====

```
   155  
   156        <!-- 系统提示词 -->
   157        <div class="config-section" style="margin-bottom: 20px;">
   158          <label style="display: block; margin-bottom: 8px; color: #a0a0a0;">身外化身性格设定 (System Prompt)</label>
   159          <textarea id="prompt-text" placeholder="定义身外化身的性格、说话方式和行为准则..." style="width: 100%; height: 100px; padding: 10px; border-radius: 8px; border: 1px solid #4a4a6a; background: #2a2a4a; color: #fff; resize: vertical;"></textarea>
   160          <div style="display: flex; gap: 10px; margin-top: 10px; align-items: center;">
   161            <button id="save-prompt-text" style="padding: 8px 16px; background: #5a5a8a; border: none; border-radius: 6px; color: #fff; cursor: pointer;">保存文本</button>
   162            <span>或</span>
   163            <input type="file" id="prompt-input" accept=".md,.txt" style="display: none;">
   164            <button onclick="document.getElementById('prompt-input').click()" style="padding: 8px 16px; background: #4a4a6a; border: none; border-radius: 6px; color: #fff; cursor: pointer;">上传文件</button>
   165            <span id="system-prompt-status" style="color: #7ec8e3;"></span>
   166          </div>
   167        </div>
   168  
   169        <!-- 初次见面语 -->
   170        <div class="config-section" style="margin-bottom: 20px;">
   171          <label style="display: block; margin-bottom: 8px; color: #a0a0a0;">初次见面语</label>
   172          <textarea id="greeting-text" placeholder="当用户第一次进入时，身外化身说的第一句话..." style="width: 100%; height: 60px; padding: 10px; border-radius: 8px; border: 1px solid #4a4a6a; background: #2a2a4a; color: #fff; resize: vertical;">欢迎来到这片充满魔法的森林！(^_^) 我是森林精灵絮语，很高兴遇见你~</textarea>
   173          <button id="save-greeting" style="margin-top: 10px; padding: 8px 16px; background: #5a5a8a; border: none; border-radius: 6px; color: #fff; cursor: pointer;">保存</button>
   174        </div>
   175  
   176        <!-- AI模型选择 -->
   177        <div id="llm-selector-container" class="config-section" style="margin-bottom: 20px; display: none;">
   178          <label style="display: block; margin-bottom: 8px; color: #a0a0a0;">AI 模型选择</label>
   179          <div style="display: flex; gap: 10px; align-items: center; flex-wrap: wrap;">
   180            <select id="llm-selector" style="flex: 1; min-width: 150px; padding: 10px; border-radius: 8px; border: 1px solid #4a4a6a; background: #2a2a4a; color: #fff; cursor: pointer;">
   181            </select>
   182            <button id="test-llm-btn" style="padding: 10px 16px; background: #4a6a4a; border: none; border-radius: 8px; color: #fff; cursor: pointer; white-space: nowrap;">测试连接</button>
   183          </div>
   184          <div id="llm-test-status" style="margin-top: 8px; font-size: 13px; min-height: 20px;"></div>
   185          <p style="font-size: 12px; color: #666; margin-top: 5px;">选择身外化身使用的AI大脑</p>
   186        </div>
   187  
   188        <div style="text-align: center; padding-top: 10px; border-top: 1px solid #4a4a6a;">
   189          <p style="font-size: 12px; color: #666;">上传的资源会自动应用到你的世界中</p>
   190        </div>
   191      </div>
   192    </div>
   193  
   194    <!-- 拍照预览弹窗 -->
   195    <div id="photo-modal" style="display: none; position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; background: rgba(0,0,0,0.9); z-index: 9999999; justify-content: center; align-items: center; flex-direction: column;">
   196      <div style="position: relative; max-width: 90vw; max-height: 80vh;">
   197        <img id="photo-preview" style="max-width: 100%; max-height: 70vh; border-radius: 8px; box-shadow: 0 10px 40px rgba(0,0,0,0.5);">
   198        <div style="display: flex; gap: 15px; justify-content: center; margin-top: 20px;">
   199          <button id="download-photo" style="padding: 12px 24px; background: #4ade80; border: none; border-radius: 8px; color: #000; font-size: 16px; cursor: pointer; display: flex; align-items: center; gap: 8px;">
   200            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24"><path fill="currentColor" d="M5 20h14v-2H5v2zm7-18l-5 5h3v9h4V7h3l-5-5z" transform="rotate(180 12 12)"/></svg>
   201            下载照片
   202          </button>
   203          <button id="close-photo" style="padding: 12px 24px; background: #6b7280; border: none; border-radius: 8px; color: #fff; font-size: 16px; cursor: pointer;">关闭</button>
   204        </div>
```

===== 第55页 =====

```
   205      </div>
   206    </div>
   207  
   208    <!-- 分享弹窗 -->
   209    <div id="share-modal" style="display: none; position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; background: rgba(0,0,0,0.8); z-index: 9999999; justify-content: center; align-items: center;">
   210      <div style="background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%); padding: 30px; border-radius: 16px; max-width: 450px; width: 90%; color: #fff; box-shadow: 0 20px 60px rgba(0,0,0,0.5);">
   211        <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px;">
   212          <h2 style="margin: 0; color: #d4af37;">分享我的世界</h2>
   213          <button id="close-share" style="background: none; border: none; color: #fff; font-size: 24px; cursor: pointer;">&times;</button>
   214        </div>
   215  
   216        <div id="share-content">
   217          <p style="color: #a0a0a0; margin-bottom: 15px;">生成专属分享链接，让朋友也能体验你的世界！</p>
   218  
   219          <div id="share-link-container" style="display: none;">
   220            <label style="display: block; margin-bottom: 8px; color: #a0a0a0;">分享链接</label>
   221            <div style="display: flex; gap: 10px; margin-bottom: 15px;">
   222              <input type="text" id="share-link" readonly style="flex: 1; padding: 12px; border-radius: 8px; border: 1px solid #4a4a6a; background: #2a2a4a; color: #fff; font-size: 14px;">
   223              <button id="copy-link" style="padding: 12px 16px; background: #4a6a4a; border: none; border-radius: 8px; color: #fff; cursor: pointer; white-space: nowrap;">复制</button>
   224            </div>
   225  
   226            <label style="display: block; margin-bottom: 8px; color: #a0a0a0;">分享文案</label>
   227            <textarea id="share-text" readonly style="width: 100%; height: 80px; padding: 12px; border-radius: 8px; border: 1px solid #4a4a6a; background: #2a2a4a; color: #fff; font-size: 14px; resize: none;"></textarea>
   228            <button id="copy-all" style="margin-top: 10px; padding: 12px 20px; background: #d4af37; border: none; border-radius: 8px; color: #000; font-size: 14px; cursor: pointer; width: 100%;">一键复制链接和文案</button>
   229  
   230            <div id="share-stats" style="margin-top: 15px; padding: 10px; background: rgba(255,255,255,0.05); border-radius: 8px; text-align: center;">
   231              <span style="color: #7ec8e3;">已被浏览 <span id="view-count">0</span> 次</span>
   232            </div>
   233          </div>
   234  
   235          <div id="share-generate" style="text-align: center;">
   236            <button id="generate-share" style="padding: 15px 30px; background: linear-gradient(135deg, #d4af37, #f0c14b); border: none; border-radius: 8px; color: #000; font-size: 16px; cursor: pointer; font-weight: bold;">生成分享链接</button>
   237          </div>
   238        </div>
   239  
   240        <div id="share-status" style="margin-top: 15px; text-align: center; min-height: 24px;"></div>
   241      </div>
   242    </div>
   243  
   244    <!-- 访客模式提示条 -->
   245    <div id="visitor-banner" style="display: none; position: fixed; top: 0; left: 0; right: 0; background: linear-gradient(90deg, #d4af37, #f0c14b); color: #000; padding: 10px 20px; z-index: 9999998; text-align: center; font-size: 14px;">
   246      <span id="visitor-world-name">正在浏览分享的世界</span>
   247      <button id="create-own-world" style="margin-left: 15px; padding: 5px 15px; background: #1a1a2e; border: none; border-radius: 4px; color: #fff; cursor: pointer; font-size: 13px;">创建我的世界</button>
   248    </div>
   249  
   250    <a-scene device-orientation-permission-ui="enabled: false" loading-screen="enabled: true"
   251      xr-mode-ui="enterVRButton: #myEnterVRButton; enterARButton: #myEnterARButton; XRMode: none">
   252  
   253      <!-- 资产管理部分 -->
   254      <a-assets>
```

===== 第56页 =====

```
   255        <!-- 环境贴图 -->
   256        <img id="skyTexture" src="./yard2_compressed.jpg">
   257  
   258        <!-- 3D模型 -->
   259        <a-asset-item id="fairyModel" src="./fairy_compressed.gltf"></a-asset-item>
   260      </a-assets>
   261  
   262      <!-- 天空盒 - 默认隐藏，等待world-config.js加载配置后显示 -->
   263      <a-sky id="sky" src="#skyTexture" rotation="0 -90 0" visible="false"></a-sky>
   264  
   265      <!-- 相机装备 -->
   266      <a-entity id="rig" position="0 1.6 0">
   267        <a-camera wasd-controls="acceleration: 200" look-controls>
   268          <!-- 身外化身 - 默认不加载模型，由world-config.js根据用户配置决定 -->
   269          <a-entity id="fairy" position="-1 -0.1 -2" rotation="0 -120 0" scale="1 1 1" visible="false"
   270            animation__rotate="property: rotation;
   271                            to: 0 360000 0;
   272                            loop: true;
   273                            dur: 36000000;
   274                            easing: linear">
   275          </a-entity>
   276  
   277        </a-camera>
   278      </a-entity>
   279    </a-scene>
   280  
   281    <script src="./component.js"></script>
   282    <script src="./chat.js"></script>
   283    <script src="./world-config.js"></script>
   284    <script>
   285    </script>
   286  </body>
   287  
   288  </html>```

### 文件: init.js

**路径:** ../frontend/init.js  
**说明:** 初始化脚本  
**行数:** 28行

```
     1  
     2  // 打印天空盒的大小
     3  const skyScale = document.querySelector('#sky').getAttribute('scale');
     4  console.log('Skybox Scale:', skyScale); // 例如：{ x: 100, y: 100, z: 100 }
     5  
     6  // 获取模型并计算边界盒的大小
     7  function getModelSize(modelId) {
     8      const modelEl = document.querySelector(`#${modelId}`);
     9      
    10      // 等待模型加载完成
    11      modelEl.addEventListener('loaded', () => {
    12          const object3D = modelEl.object3D;
    13          const boundingBox = new THREE.Box3().setFromObject(object3D); // 获取模型的边界盒
    14          const size = new THREE.Vector3();
    15          boundingBox.getSize(size); // 获取尺寸（宽度、深度、高度）
    16          
```

===== 第57页 =====

```
    17          console.log(`${modelId} Size:`);
    18          console.log(`Width: ${size.x}, Height: ${size.y}, Depth: ${size.z}`);
    19      });
    20  }
    21  
    22  // 打印所有模型的大小
    23  getModelSize('fairyModel');
    24  getModelSize('dropletModel');
    25  getModelSize('flowerModel');
    26  getModelSize('stoneModel');
    27  getModelSize('petalModel');
    28  getModelSize('butterflyModel');
```


---

**源代码文档生成完毕**

- 总行数: 2812行
- 总页数: 57页
- 前30页: 第1-1500行
- 后30页: 第1501-3000行
